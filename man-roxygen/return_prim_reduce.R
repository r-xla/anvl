#' @return [`arrayish`]\cr
#'   Has the same data type as the input.
#'   When `drop = TRUE`, the shape is that of `operand` with `axes` removed.
#'   When `drop = FALSE`, the shape is that of `operand` with `axes` set to 1.
#'   It is ambiguous if the input is ambiguous.
