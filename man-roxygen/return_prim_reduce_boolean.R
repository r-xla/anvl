#' @return [`arrayish`]\cr
#'   Boolean array. Never ambiguous.
#'   When `drop = TRUE`, the shape is that of `x` with `dims` removed.
#'   When `drop = FALSE`, the shape is that of `x` with `dims` set to 1.
