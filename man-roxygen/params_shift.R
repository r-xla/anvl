#' @param x ([`arrayish`])\cr
#'   The array whose bits are shifted. Can be any integer data type.
#' @param shift ([`arrayish`])\cr
#'   By how many bits to shift each element of `x`. Brought to `x`'s data type,
#'   which it must fit in. Scalars are broadcast.
