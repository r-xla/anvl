#' @return (`list` of two [`arrayish`])\cr
#'   Unnamed. The running <%= cum_extreme_name %>, at the input's data type,
#'   and the running <%= cum_extreme_arg %>, at `i32`. Both have the input's
#'   shape.
