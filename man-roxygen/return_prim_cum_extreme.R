#' @return Unnamed `list` of two [`arrayish`] values:\cr
#'   The running <%= cum_extreme_name %>, at the input's data type, and the
#'   running <%= cum_extreme_arg %>, at `i32`. Both have the input's shape.
