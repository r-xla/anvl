#' @return `list` of two [`arrayish`] values:\cr
#'   The running <%= cum_extreme_name %> (same dtype as `x`) and the
#'   running <%= cum_extreme_arg %> (of the default integer data
#'   type -- see [`default_dtypes()`]). Both have the same shape as `x`.
