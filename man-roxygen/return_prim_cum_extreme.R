#' @return `list` of two [`arrayish`] values:\cr
#'   The running <%= cum_extreme_name %> (same dtype as `x`) and the
#'   running <%= cum_extreme_arg %> (dtype `i32`, 1-based). Both have
#'   the same shape as `x`.
