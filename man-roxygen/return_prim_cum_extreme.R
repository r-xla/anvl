#' @return (named `list` of two [`arrayish`])\cr
#'   Elements `values`, the running <%= cum_extreme_name %> at the input's data
#'   type, and `indices`, the running <%= cum_extreme_arg %> at the default
#'   integer data type (see [`default_dtypes()`]). Both have the input's shape.
