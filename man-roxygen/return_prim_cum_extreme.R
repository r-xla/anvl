#' @return (`list` of two [`arrayish`])\cr
#'   Unnamed. The running <%= cum_extreme_name %>, at the input's data type,
#'   and the running <%= cum_extreme_arg %>, at the [default integer data type][default_dtypes].
#'   Both have the input's shape.
