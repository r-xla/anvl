# Test whether an object is a device

Test whether an object is a device

## Usage

``` r
is_device(x)
```

## Arguments

- x:

  An object to test.

## Value

(`logical(1)`)  
Whether `x` is a device of one of the backends.
