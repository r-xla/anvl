% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_int_div}
\alias{nv_int_div}
\title{Integer Division}
\usage{
nv_int_div(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Left and right operand.
Operands are \link[=nv_promote_to_common]{promoted to a common data type}.
Scalars are \link[=nv_broadcast_scalars]{broadcast} to the shape of the other operand.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape and the promoted common data type of the inputs.
}
\description{
Element-wise flooring division, matching base R's \code{\%/\%} operator: the
result is the largest whole number that does not exceed \code{lhs / rhs}.
You can also use the \code{\%/\%} operator.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(7L, -7L))
y <- nv_array(c(2L, 2L))
nv_int_div(x, y)
as.vector(x) \%/\% as.vector(y)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_mod]{nv_mod()}} for the matching remainder, \code{\link[=nv_div]{nv_div()}} for the
division itself.
}
