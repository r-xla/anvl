% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_argmin}
\alias{nv_argmin}
\title{Index of the Minimum}
\usage{
nv_argmin(operand, dim = NULL, drop = TRUE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Operand.}

\item{dim}{(\code{integer(1)} | \code{NULL})\cr
Dimension along which to find the index. If \code{NULL} (default), uses
the last dimension.}

\item{drop}{(\code{logical(1)})\cr
If \code{TRUE} (default) the reduced dimension is removed; if \code{FALSE} it
is kept with size 1.}
}
\value{
\code{\link{arrayish}} of dtype \code{i32}\cr
Same shape as \code{operand} with \code{dim} removed (or set to 1 if \code{drop = FALSE}).
}
\description{
Returns the index of the minimum value along a dimension. Ties are broken
by returning the smallest index.
}
\section{NaN handling}{

Like \code{\link[=nv_argmax]{nv_argmax()}}, \code{NaN} entries are skipped by the comparison-based XLA
kernel; the returned index refers to the smallest non-\code{NaN} value. This
matches base R's \code{which.min(c(1, NaN, 3))} and JAX's \code{jnp.argmin}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_argmin(nv_array(c(3, 1, 4, 1, 5, 9, 2, 6)))
nv_argmin(nv_array(c(2, NaN, 1, 3)))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_argmax]{nv_argmax()}}, \code{\link[=nv_reduce_min]{nv_reduce_min()}}.
}
