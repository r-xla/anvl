% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_reduce_sum}
\alias{nv_reduce_sum}
\title{Sum Reduction}
\usage{
nv_reduce_sum(x, axes = NULL, drop = TRUE, nan_rm = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reduce. Negative values count from the end, i.e. \code{-1}
refers to the last axis. If \code{NULL} (default), reduces over all
axes, returning a scalar.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop reduced axes.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in floating-point inputs. If \code{FALSE}
(default), \code{NaN} propagates. If \code{TRUE}, \code{NaN} values are skipped.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input, except for a boolean input, which is
accumulated at \code{i32}: \code{TRUE} counts as one, rather than being folded with
a logical or/and.
When \code{drop = TRUE}, the reduced axes are removed.
When \code{drop = FALSE}, the reduced axes are set to 1.
}
\description{
Sums array elements along the specified axes.
A boolean array is counted, like \code{\link[base:sum]{base::sum()}} does.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(1:6, nrow = 2)
nv_reduce_sum(x)            # all axes -> scalar
nv_reduce_sum(x, axes = 1L)
nv_reduce_sum(nv_array(c(1, NaN, 3)))
nv_reduce_sum(nv_array(c(1, NaN, 3)), nan_rm = TRUE)
nv_reduce_sum(nv_array(c(TRUE, FALSE, TRUE))) # counts: 2
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_reduce_sum]{prim_reduce_sum()}} for the underlying primitive.
}
