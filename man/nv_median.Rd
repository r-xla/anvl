% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_median}
\alias{nv_median}
\alias{median.AnvlArray}
\title{Median}
\usage{
nv_median(x, axes = NULL, drop = TRUE, method = "linear", nan_rm = FALSE)

\method{median}{AnvlArray}(x, na.rm = FALSE, ..., axes = NULL, drop = TRUE, method = "linear")
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value materializes at its
\link[=default_dtypes]{default data type}.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reduce over. Negative values count from the end, i.e. \code{-1} refers
to the last axis. If \code{NULL} (default), reduces over all axes.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced axes: removed from the output shape if \code{TRUE},
set to 1 if \code{FALSE}.}

\item{method}{(\code{character(1)})\cr
Forwarded to \code{\link[=nv_quantile]{nv_quantile()}}. One of \code{"linear"} (default), \code{"lower"},
\code{"higher"}, \code{"nearest"}, \code{"midpoint"}.}

\item{nan_rm}{(\code{logical(1)})\cr
Forwarded to \code{\link[=nv_quantile]{nv_quantile()}}. See its documentation for details.}

\item{na.rm}{Forwarded to \code{\link[=nv_median]{nv_median()}}'s \code{nan_rm} argument.}

\item{...}{No additional arguments.}
}
\value{
(\code{\link{arrayish}})\cr
Same shape as \code{x} with \code{axes} removed (or set to 1 if \code{drop = FALSE}).
The data type is that of \code{x}, or the default float for a non-float \code{x}.
}
\description{
Computes the median over one or more axes. Equivalent to
\code{nv_quantile(x, 0.5, axes, drop, method)}; for an even number of
reduced elements with the default \code{"linear"} interpolation, the average of
the two middle values is returned, matching base R's \code{median()}.

You can also use \code{median()} directly on an \code{\link{AnvlArray}} or \code{\link{AnvlBox}};
extra arguments (e.g. \code{method}) are forwarded via \code{...}.
}
\section{The \code{median()} generic}{

\code{\link[stats:median]{stats::median()}} reduces every axis of a multi-axis array, and so does
\code{nv_median()} by default, so the two agree. Pass \code{axes} to reduce a subset
instead. A non-float \code{x} is computed at the default float, like base R
returns a double.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_median(nv_array(c(3, 1, 4, 1, 5, 9, 2, 6)))
median(nv_array(c(3, 1, 4, 1, 5, 9, 2, 6)))
m <- nv_matrix(c(3, 1, 5, 2, 4, 0), nrow = 2, byrow = TRUE)
nv_median(m) # over every element
nv_median(m, axes = 2L) # one median per row
# forwards through the S3 generic via `...`
median(nv_array(c(1, 2, 3, 4)), method = "lower")
nv_median(nv_array(c(1, NaN, 3, 5)))
nv_median(nv_array(c(1, NaN, 3, 5)), nan_rm = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_quantile]{nv_quantile()}}, \code{\link[=nv_sort]{nv_sort()}}, \code{\link[=prim_sort]{prim_sort()}}.
}
