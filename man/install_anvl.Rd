% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend.R
\name{install_anvl}
\alias{install_anvl}
\title{Install what a backend needs to run}
\usage{
install_anvl(backend = default_backend(), ...)
}
\arguments{
\item{backend}{(\code{character(1)})\cr
Backend to install for. Defaults to \code{\link[=default_backend]{default_backend()}}. The \code{"plain"}
backend has nothing to install and is not accepted.}

\item{...}{Passed to the underlying installer: \code{\link[pjrt:install_pjrt]{pjrt::install_pjrt()}} for
\code{"pjrt"}, \code{\link[utils:install.packages]{utils::install.packages()}} for \code{"quickr"}.}
}
\value{
\code{NULL}, invisibly. Called for its side effect.
}
\description{
A backend needs more than the packages anvl declares as dependencies: the
\code{"pjrt"} backend runs on PJRT plugins that are downloaded rather than shipped
with the package, and the \code{"quickr"} backend needs \CRANpkg{quickr}, which is only
suggested. This installs whichever of the two the given backend is missing.
}
\details{
The PJRT plugins are downloaded on demand, but not silently: the first time a
plugin is needed, an interactive session asks for confirmation, while a
non-interactive session does not download at all. Call this to make the
download an explicit step instead, for instance in a \code{Dockerfile} layer of its
own or at the start of a script that later runs unattended. The \code{PJRT_INSTALL}
environment variable overrides the prompt: \code{"1"} always downloads without
asking, \code{"0"} never downloads.

Which plugins you get -- and whether CUDA is available at all -- is decided
by the repository anvl was installed from, not by this call. See the
installation vignette: \code{vignette("installation", package = "anvl")}.
}
