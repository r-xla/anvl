% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_reverse}
\alias{nv_reverse}
\alias{rev.AnvlArray}
\title{Reverse}
\usage{
nv_reverse(x, axes = NULL)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value materializes at its
\link[=default_dtypes]{default data type}.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reverse. Negative values count from the end, i.e. \code{-1} refers to
the last axis. If \code{NULL} (default), reverses along every axis.}
}
\value{
(\code{\link{arrayish}})\cr
Has the same shape and data type as \code{x}.
}
\description{
Reverses the order of elements along the given axes, every axis by default.
You can also use \code{rev()}, which always reverses along every axis.
}
\section{The \code{rev()} generic}{

\code{rev()} reverses along every axis, which puts the elements in the same
order as \code{\link[base:rev]{base::rev()}} does (base R flattens the array to a vector first,
whereas \code{rev()} on an anvl array keeps the shape).
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# the order along axis 1 is flipped
x <- nv_array(c(1, 2, 3, 4, 5))
nv_reverse(x)

m <- nv_matrix(1:6, nrow = 2)
nv_reverse(m) # every axis
nv_reverse(m, axes = 2L) # columns only
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_reverse]{prim_reverse()}} for the underlying primitive.
}
