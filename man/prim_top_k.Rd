% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_top_k}
\alias{prim_top_k}
\title{Primitive Top-K}
\usage{
prim_top_k(x, k, indices = TRUE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Array of integer, unsigned integer, or floating-point dtype with rank >= 1.}

\item{k}{(\code{integer(1)})\cr
Number of top elements. Must satisfy
\verb{1 <= k <= shape(x)[naxes(x)]}.}

\item{indices}{(\code{logical(1)})\cr
Whether to also return the indices of the top elements. Without them
the order among tied values is unspecified, which lets the lowering
pick the cheapest selection for the platform.}
}
\value{
\code{list} of one or two \code{\link{arrayish}} values:\cr
The top-\code{k} values (same dtype as \code{x}) and, if \code{indices} is \code{TRUE},
their indices along the last axis, of the default integer data type
(see \code{\link[=default_dtypes]{default_dtypes()}}). Both have the same shape as \code{x} with the
last axis replaced by \code{k}. With indices, ties are broken by lower
index first.
}
\description{
Returns the \code{k} largest values along the last axis, sorted in
descending order, together with their indices into that axis.

For other axes, transpose so the target axis is last, call
\code{prim_top_k()}, then transpose back. \code{\link[=nv_top_k]{nv_top_k()}} does this.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_top_k]{hlo_top_k()}}. Without \code{indices} on CUDA it lowers to an
unstable descending \code{\link[stablehlo:hlo_sort]{hlo_sort()}} of the values followed by an
\code{\link[stablehlo:hlo_slice]{hlo_slice()}}, which is what the CHLO op expands to there minus the
index operand and the stability the ties no longer need; XLA's CPU
backend has a dedicated top-k kernel, so it keeps \code{\link[stablehlo:hlo_top_k]{hlo_top_k()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(3, 1, 4, 1, 5, 9, 2, 6))
prim_top_k(x, k = 3L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_top_k]{nv_top_k()}}, \code{\link[=prim_sort]{prim_sort()}}
}
