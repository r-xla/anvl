% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array.R
\name{ConcreteArray}
\alias{ConcreteArray}
\title{Concrete Array Class}
\usage{
ConcreteArray(data)
}
\arguments{
\item{data}{(\code{\link{AnvlArray}})\cr
The actual array data.}
}
\description{
An \code{\link{AbstractArray}} that also holds a reference to the actual array data.
Usually represents a closed-over constant in a program.
Inherits from \code{\link{AbstractArray}}.
}
\section{Lowering}{

When lowering to XLA, these become inputs to the executable instead of embedding them into
programs as constants.
This is to avoid increasing compilation time and bloating the size of the executable.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
y <- nv_array(c(0.5, 0.6))
x <- ConcreteArray(y)
x
shape(x)
naxes(x)
dtype(x)

# How it appears during tracing
graph <- trace_fn(function() y, list())
graph
graph$outputs[[1]]$aval
\dontshow{\}) # examplesIf}
}
