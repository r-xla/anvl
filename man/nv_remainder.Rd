% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_remainder}
\alias{nv_remainder}
\title{Remainder (Truncating)}
\usage{
nv_remainder(x, y)
}
\arguments{
\item{x, y}{(\code{\link{arrayish}})\cr
Two inputs with a \link[=common_dtype]{common data type}. Can be
any numeric data type. Scalars are broadcast. An R value takes the other
operand's data type when that is in its own or a higher
\link[=dtypes]{category} (an R integer meeting \code{f64} becomes \code{f64}); otherwise it
settles on its \link[=default_dtypes]{default data type}, and the operands meet
at their common data type.}
}
\value{
(\code{\link{arrayish}})\cr
Has the inputs' broadcast shape and common data type.
}
\description{
Element-wise remainder. This
differs from base R's \code{\%\%}, use \code{\link[=nv_mod]{nv_mod()}}/\code{\%\%} instead.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(7, 8, 9))
y <- nv_array(c(3, 3, 4))
nv_remainder(x, y)

# different data types are promoted to their common one
nv_remainder(nv_scalar(7, "f32"), nv_scalar(3, "f64"))

# a scalar is broadcast and an R integer is converted to a float
nv_remainder(x, 3L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_mod]{nv_mod()}} for the flooring remainder, \code{\link[=prim_remainder]{prim_remainder()}} for the
underlying primitive.
}
