% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_round}
\alias{nv_round}
\alias{round.AnvlArray}
\title{Round}
\usage{
nv_round(x, method = "nearest_even")

\method{round}{AnvlArray}(x, digits = 0, ...)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array. An integer array is returned unchanged, like in base R; a
boolean array is not accepted.}

\item{method}{(\code{character(1)})\cr
Rounding method.
Either \code{"nearest_even"} (default) or \code{"afz"} (away from zero).}

\item{digits}{(\code{numeric(1)})\cr
Number of digits to round to, as in \code{\link[base:round]{base::round()}}. Must be a plain R
value.}

\item{...}{Further arguments of \code{\link[=nv_round]{nv_round()}}.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape and data type as the input.
}
\description{
Element-wise rounding. You can also use the \code{round()} generic.
}
\section{Relation to base R}{

\code{round(x, digits)} is computed by scaling with \code{10^digits}, so it can differ
from \code{\link[base:round]{base::round()}} in the last representable digit. A negative \code{digits}
rounds an integer array to a multiple of ten in base R; on an anvl array,
where an integer never turns into a float on its own, that is an error
instead.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1.4, 2.5, 3.6))
round(x)
round(nv_array(1:3)) # an integer array is already whole
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_round]{prim_round()}} for the underlying primitive.
}
