% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-like.R
\name{nv_linspace}
\alias{nv_linspace}
\alias{nv_linspace_like}
\title{Evenly Spaced Sequence}
\usage{
nv_linspace(start, end, steps, dtype = NULL, device = NULL)

nv_linspace_like(like, start, end, steps, dtype = NULL, device = NULL)
}
\arguments{
\item{start, end}{(\code{numeric(1)})\cr
First and last value of the sequence. \code{end} may lie below \code{start}, in
which case the values decrease.}

\item{steps}{(\code{integer(1)})\cr
Number of values to generate. Must be at least 1; for \code{steps = 1} the
result is \code{start}.}

\item{dtype}{(\code{NULL} | \code{character(1)} | \code{\link[tengen:DataType]{DataType}})\cr
Data type of the result. Must be \code{f32} or \code{f64}. The default (\code{NULL}) is
the \link[=default_dtypes]{default data type} of an R double, since the spacing
is fractional. For \code{nv_linspace_like()}, \code{NULL} uses \code{dtype(like)}, which
must then be one of those two. To obtain integers, convert the result with
\code{\link[=nv_convert]{nv_convert()}}, which leaves the rounding yours to choose.}

\item{device}{(\code{NULL} | \code{character(1)} | \link[=nv_device]{device})\cr
The device the data lives on, given either as:
\itemize{
\item a \emph{device string} naming the platform (e.g. \code{"cpu"}, \code{"cuda"},
\code{"cuda:<n>"}), which is resolved against the backend in use, or
\item a \emph{device object} as returned by \code{\link[=nv_device]{nv_device()}}: a
\code{\link[pjrt:pjrt_device]{PJRTDevice}} for the \code{"pjrt"} backend or a
\code{\link{quickr_device}} for the \code{"quickr"} backend. Because a device object
is backend-specific, it also determines the backend.
}

The default (\code{NULL}) uses \code{\link[=default_device]{default_device()}}: the CPU, or the platform
named by the \code{PJRT_PLATFORM} environment variable on the \code{"pjrt"} backend.}

\item{like}{(\code{\link{AnvlArray}})\cr
Existing array whose attributes are used as defaults
(only for \code{nv_linspace_like()}).}
}
\value{
(\code{\link{arrayish}})\cr
Has \code{dtype} and shape \code{steps}.
}
\description{
Creates a 1-D array with \code{steps} evenly spaced values from \code{start} to \code{end}
(both inclusive), like R's \code{seq(start, end, length.out = steps)}.

The spacing \code{(end - start) / (steps - 1)} is generally not a whole number,
so the result is floating-point.

\code{nv_linspace_like()} is a variant where \code{dtype} and \code{device}
default to those of \code{like}.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_linspace(0, 1, steps = 5L)

# end below start counts down
nv_linspace(1, 0, steps = 3L)

# steps = 1 gives start alone
nv_linspace(2.5, 10, steps = 1L)

# the data type must be a float; convert afterwards for integers
nv_convert(nv_linspace(0, 10, steps = 5L), "i32")

# nv_linspace_like() takes dtype and device from an existing array
x <- nv_array(c(1, 2, 3), dtype = "f64")
nv_linspace_like(x, 0, 1, steps = 3L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_seq]{nv_seq()}} for consecutive integers, \code{\link[=nv_iota]{nv_iota()}} for values
increasing along an axis of any shape, \code{\link{dtypes}} for the data type
categories.
}
