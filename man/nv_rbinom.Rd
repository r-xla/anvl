% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-rng.R
\name{nv_rbinom}
\alias{nv_rbinom}
\title{Sample from a Binomial Distribution}
\usage{
nv_rbinom(shape, initial_state, size = 1L, prob = 0.5, dtype = "i32")
}
\arguments{
\item{shape}{(\code{integer()})\cr
Shape.}

\item{initial_state}{(\code{\link{arrayish}})\cr
RNG state (\code{ui64[2]}).}

\item{size}{(\code{integer(1)})\cr
Number of trials. A plain R number rather than \code{\link{arrayish}}, as is \code{prob},
so nothing is promoted.}

\item{prob}{(\code{numeric(1)})\cr
Probability of success on each trial.}

\item{dtype}{(\code{character(1)} | \code{\link[tengen:DataType]{DataType}})\cr
Data type of the sample. Can be any numeric data type; the successes are
counted and converted to it. Boolean is not one, and is rejected: it
cannot hold a count.}
}
\value{
(named \code{list} of two \code{\link{arrayish}})\cr
Elements \code{state}, the updated RNG state, and \code{values}, the sample of shape
\code{shape} and data type \code{dtype}.
}
\description{
Samples from a binomial distribution with \eqn{n} trials and success probability \eqn{p}.
When \code{size = 1} (the default), this is a Bernoulli distribution.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# Bernoulli samples; `state` is the updated RNG state
state <- nv_rng_state(42L)
result <- nv_rbinom(c(2, 3), state)
result$values
\dontshow{\}) # examplesIf}
}
\seealso{
Other rng:
\code{\link{nv_normal}},
\code{\link[=nv_rng_state]{nv_rng_state()}},
\code{\link[=nv_runif]{nv_runif()}},
\code{\link[=nv_sample]{nv_sample()}},
\code{\link[=nv_sample_int]{nv_sample_int()}}
}
\concept{rng}
