% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_reduce_min}
\alias{nv_reduce_min}
\title{Min Reduction}
\usage{
nv_reduce_min(operand, axes = NULL, drop = TRUE, nan_rm = FALSE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Operand.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reduce. If \code{NULL} (default), reduces over all
axes, returning a scalar.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop reduced axes.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in floating-point inputs. If \code{FALSE}
(default), \code{NaN} propagates. If \code{TRUE}, \code{NaN} values are skipped.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input.
When \code{drop = TRUE}, the reduced axes are removed.
When \code{drop = FALSE}, the reduced axes are set to 1.
}
\description{
Finds the minimum of array elements along the specified axes.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(1:6, nrow = 2)
nv_reduce_min(x)            # all axes -> scalar
nv_reduce_min(x, axes = 1L)
nv_reduce_min(nv_array(c(1, NaN, 3)))
nv_reduce_min(nv_array(c(1, NaN, 3)), nan_rm = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_reduce_min]{prim_reduce_min()}} for the underlying primitive.
}
