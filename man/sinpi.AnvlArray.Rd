% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-generics.R
\name{sinpi.AnvlArray}
\alias{sinpi.AnvlArray}
\title{Sine of a Multiple of Pi}
\usage{
\method{sinpi}{AnvlArray}(x)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array. An integer array is computed at the default float data type
(see \code{\link[=default_dtypes]{default_dtypes()}}), the way \code{\link[base:sqrt]{base::sqrt()}} returns a double for an
integer vector. A boolean array is not accepted.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape as the input, and its data type -- or the default float
data type if the input was an integer array.
}
\description{
Element-wise \code{sin(pi * x)}, the generic \code{\link[base:sinpi]{base::sinpi()}} on an anvl array.
Like base R's, it is exact for a whole or half-integer argument: the
argument is first reduced to the interval \verb{[-0.5, 0.5]} around the nearest
whole number, which is where the sine of a multiple of pi is accurate.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
sinpi(nv_array(c(0, 0.5, 1, 1.5)))
\dontshow{\}) # examplesIf}
}
\seealso{
\link[=cospi.AnvlArray]{cospi()}, \link[=tanpi.AnvlArray]{tanpi()}, \code{\link[=nv_sin]{nv_sin()}}
}
