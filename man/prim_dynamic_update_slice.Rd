% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_dynamic_update_slice}
\alias{prim_dynamic_update_slice}
\title{Primitive Dynamic Update Slice}
\usage{
prim_dynamic_update_slice(x, update, ...)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
The array to write into. Can be any data type.
\code{x} and \code{update} must have the same data type. An R value among them assumes the data type of the others when it is in its \link[=dtypes]{data type category}, and its \link[=default_dtypes]{default data type} when none of them has one.}

\item{update}{(\code{\link{arrayish}})\cr
The values to write at the specified position. Must have the same
number of axes as \code{x}, with \code{shape(update) <= shape(x)} per axis.
Shares \code{x}'s data type -- see \code{x}.}

\item{...}{(\code{\link{arrayish}})\cr
Scalar start indices, one per axis of \code{x}. Each must be a scalar of an
integer data type, and keeps it -- the indices take no part in \code{x}'s.}
}
\value{
(\code{\link{arrayish}})\cr
Has the same data type and shape as \code{x}.
}
\description{
Returns a copy of \code{x} with a slice replaced by \code{update} at a
runtime-determined position. This is the write counterpart of
\code{\link[=prim_dynamic_slice]{prim_dynamic_slice()}}: dynamic slice reads a block from an array,
while dynamic update slice writes a block into an array.
}
\section{Out of Bounds Behavior}{

Start indices are clamped before the update is written:
\code{adjusted_start_indices = clamp(1, start_indices, shape(x) - shape(update) + 1)}.
This means that out-of-bounds indices will not cause an error, but the
effective start position may differ from the requested one.
}

\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_dynamic_update_slice]{hlo_dynamic_update_slice()}}, specified under \href{https://openxla.org/stablehlo/spec#dynamic_update_slice}{dynamic_update_slice}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# 1-D: overwrite two elements starting at position 2
x <- nv_array(1:5)
update <- nv_array(c(10L, 20L))
start <- nv_scalar(2L)
prim_dynamic_update_slice(x, update, start)

# 2-D: write a 2x2 block into a 3x4 matrix
x <- nv_fill(0L, shape = c(3, 4))
update <- nv_matrix(c(1L, 2L, 3L, 4L), nrow = 2, ncol = 2)
row_start <- nv_scalar(2L)
col_start <- nv_scalar(3L)
prim_dynamic_update_slice(x, update, row_start, col_start)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_dynamic_slice]{prim_dynamic_slice()}}, \code{\link[=prim_scatter]{prim_scatter()}}, \code{\link[=prim_gather]{prim_gather()}}, \code{\link[=nv_subset_assign]{nv_subset_assign()}}, \verb{[<-}
}
