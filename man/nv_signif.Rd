% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_signif}
\alias{nv_signif}
\alias{signif.AnvlArray}
\title{Round to Significant Digits}
\usage{
nv_signif(x, digits = 6)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array. Must be a float array: unlike base R, an integer array is not
rounded to a coarser magnitude, since that would have to turn it into a
float. Convert it with \code{\link[=nv_convert]{nv_convert()}} if that is what you mean.}

\item{digits}{(\code{numeric(1)})\cr
Number of significant digits, as in \code{\link[base:signif]{base::signif()}}. Must be a plain R
value; a value below 1 is raised to 1, like in base R.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape and data type as the input.
}
\description{
Element-wise rounding to \code{digits} significant digits. You can also use the
\code{signif()} generic. It is computed by rounding the mantissa, so it can
differ from base R's \code{\link[base:signif]{base::signif()}} in the last representable digit.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
signif(nv_array(c(123.456, -0.001234)), 3)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_round]{nv_round()}}, \code{\link[=round]{round()}}
}
