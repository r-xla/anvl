% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_svd}
\alias{nv_svd}
\title{Singular Value Decomposition}
\usage{
nv_svd(operand)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 dimensions.}
}
\value{
Named \code{list} with elements \code{d} (length \code{k}), \code{u} (shape
\verb{(m, k)}), and \code{vt} (shape \verb{(k, n)}). All have the same dtype as
the input.
}
\description{
Computes the reduced ("economy") singular value decomposition of a
matrix \code{operand} of shape \verb{(m, n)}:
\deqn{A = u \, \mathrm{diag}(d) \, vt,}
where \code{u} has orthonormal columns, \code{vt} has orthonormal rows, and \code{d}
is the length-\code{k} (\code{k = min(m, n)}) vector of non-negative singular
values in descending order.

Note: unlike \code{base::svd()}, which returns the right singular vectors
as \code{v} of shape \verb{(n, k)} (so that \code{a = u \%*\% diag(d) \%*\% t(v)}), this
primitive returns them already transposed as \code{vt} of shape \verb{(k, n)}
(matching the underlying LAPACK / cuSOLVER output and avoiding an
extra transpose).

Supports any matrix shape on both the host (LAPACK \code{gesdd}) and CUDA
(cuSOLVER \code{gesvd}) backends. cuSOLVER's \code{m >= n} requirement is handled
transparently via a layout flip for wide matrices.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(1, 0, 0, 1, 0, 1), nrow = 3, dtype = "f64")
nv_svd(x)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_svd]{prim_svd()}}, \code{\link[base:svd]{base::svd()}}
}
