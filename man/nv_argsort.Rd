% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_argsort}
\alias{nv_argsort}
\title{Argsort}
\usage{
nv_argsort(x, dim = NULL, decreasing = FALSE, stable = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array.}

\item{dim}{(\code{integer(1)} | \code{NULL})\cr
Dimension along which to compute the sort permutation. If \code{NULL}
(default), uses the last dimension.}

\item{decreasing}{(\code{logical(1)})\cr
If \code{TRUE}, returns indices that produce a decreasing sort. Default
\code{FALSE}.}

\item{stable}{(\code{logical(1)})\cr
If \code{TRUE}, the sort is stable: indices for equal values keep their
original relative order. Default \code{FALSE}.}
}
\value{
\code{\link{arrayish}} of dtype \code{i32}\cr
Same shape as \code{x}. For a size-0 axis, the output is an empty \code{i32}
array of the same shape (a valid empty permutation).
\code{as_array(x)[as_array(nv_argsort(x))]} reproduces the sorted
array (for 1-D inputs).
}
\description{
Returns the indices that would sort the array along a dimension.
}
\section{NaN handling}{

\code{NaN} values sort to the \strong{end} (ascending) or \strong{beginning}
(descending), regardless of sign. \code{+0} and \code{-0} compare equal.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(3, 1, 4, 1, 5))
nv_argsort(x)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_sort]{nv_sort()}}, \code{\link[=prim_sort]{prim_sort()}}.
}
