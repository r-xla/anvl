% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_determinant}
\alias{nv_determinant}
\alias{determinant.AnvlArray}
\title{Determinant in modulus/sign form}
\usage{
nv_determinant(x, logarithm = TRUE)

\method{determinant}{AnvlArray}(x, logarithm = TRUE, ...)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Square matrix of floating-point data type.}

\item{logarithm}{(\code{logical(1)})\cr If \code{TRUE} (default), return the log
of the absolute determinant.}

\item{...}{No additional arguments.}
}
\value{
Named \code{list} with elements \code{modulus} and \code{sign}, both scalar
\code{\link{arrayish}} with the same dtype as \code{x}. The full determinant
is \code{sign * exp(modulus)} (with \code{logarithm = TRUE}) or
\code{sign * modulus} (with \code{logarithm = FALSE}).
}
\description{
Computes the determinant of a square matrix in the modulus / sign
decomposition matching base R's \code{\link[base:determinant]{base::determinant()}}. For the plain
scalar determinant, use \code{\link[=nv_det]{nv_det()}}.
}
\details{
For computing the determinant, we use:
\deqn{P A = L U}
\deqn{\det(L) = 1}
\deqn{\det(A) = \det(U) / \det(P) = \mathrm{sign}(P^{-1}) \, \prod_i U_{ii}
  = \mathrm{sign}(P) \, \prod_i U_{ii}}

Matching base R's \code{det_ge_real}, the magnitude is computed in log
space when \code{logarithm = TRUE} (\eqn{\sum_i \log|U_{ii}|}) and as a
direct product when \code{logarithm = FALSE} (\eqn{\prod_i |U_{ii}|}).
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
a <- nv_matrix(c(4, 3, 6, 3), nrow = 2, dtype = "f64")
nv_determinant(a)
nv_determinant(a, logarithm = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_det]{nv_det()}}, \code{\link[=nv_solve]{nv_solve()}}, \code{\link[=prim_lu]{prim_lu()}}
}
