% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_argmax}
\alias{nv_argmax}
\title{Index of the Maximum}
\usage{
nv_argmax(operand, dim = NULL, drop = TRUE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Operand.}

\item{dim}{(\code{integer(1)} | \code{NULL})\cr
Dimension along which to find the index. If \code{NULL} (default), uses
the last dimension.}

\item{drop}{(\code{logical(1)})\cr
If \code{TRUE} (default) the reduced dimension is removed; if \code{FALSE} it
is kept with size 1.}
}
\value{
\code{\link{arrayish}} of dtype \code{i32}\cr
Same shape as \code{operand} with \code{dim} removed (or set to 1 if \code{drop = FALSE}).
}
\description{
Returns the index of the maximum value along a dimension. Ties are broken
by returning the smallest index.
}
\section{NaN handling}{

The underlying XLA \code{argmax} kernel is comparison-based, so \code{NaN} entries
are skipped (they compare false against everything) and the returned index
refers to the largest non-\code{NaN} value. This matches base R's
\code{which.max(c(1, NaN, 3))} and JAX's \code{jnp.argmax}. If every entry along the
reduced axis is \code{NaN}, the result is the first index along that axis.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_argmax(nv_array(c(3, 1, 4, 1, 5, 9, 2, 6)))
nv_argmax(nv_matrix(c(3, 1, 5, 2, 4, 0), nrow = 2, byrow = TRUE),
  dim = 2L
)
nv_argmax(nv_array(c(1, NaN, 3))) # 3 -- NaN is skipped
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_argmin]{nv_argmin()}}, \code{\link[=nv_reduce_max]{nv_reduce_max()}}.
}
