% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_broadcast_in_dim}
\alias{prim_broadcast_in_dim}
\title{Primitive Broadcast}
\usage{
prim_broadcast_in_dim(operand, shape, broadcast_dimensions)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Arrayish value of any data type.}

\item{shape}{(\code{integer()})\cr
Target shape. Each mapped dimension must either match the corresponding
operand dimension or the operand dimension must be 1.}

\item{broadcast_dimensions}{(\code{integer()})\cr
Maps each dimension of \code{operand} to a dimension of the output.
Must have length equal to the number of dimensions of \code{operand}.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input and the given \code{shape}.
It is ambiguous if the input is ambiguous.
}
\description{
Broadcasts an array to a new shape by replicating the data along new or size-1 dimensions.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_broadcast_in_dim]{hlo_broadcast_in_dim()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1, 2, 3))
prim_broadcast_in_dim(x, shape = c(2, 3), broadcast_dimensions = 2L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_broadcast_to]{nv_broadcast_to()}}
}
