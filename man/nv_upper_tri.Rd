% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_upper_tri}
\alias{nv_upper_tri}
\title{Upper Triangular Mask}
\usage{
nv_upper_tri(shape, diagonal = 1L, device = NULL)
}
\arguments{
\item{shape}{(\code{integer()})\cr
Shape.}

\item{diagonal}{(\code{integer(1)})\cr
Diagonal offset, with the same meaning as in \code{\link[=nv_triu]{nv_triu()}}. The default
\code{1} excludes the main diagonal, matching \code{upper.tri()}; use \code{0} to
include it, matching \code{upper.tri(diag = TRUE)}.}

\item{device}{( \code{character(1)} | \code{PJRTDevice} | \code{\link{quickr_device}} | \code{NULL})\cr
Device for data to live on.}

\item{like}{(\code{\link{AnvlArray}})\cr
Existing array whose attributes are used as defaults
(only for \code{nv_upper_tri_like()}).}
}
\value{
\code{\link{arrayish}}\cr
Has the given \code{shape} and dtype \code{bool}.
}
\description{
Returns a boolean matrix that is \code{TRUE} on and above the given diagonal,
mirroring base R's \code{upper.tri()}. Use \code{\link[=nv_triu]{nv_triu()}} to zero out the other
triangle of an existing array instead.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_upper_tri(c(3, 3))
nv_upper_tri(c(3, 3), diagonal = 0L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_lower_tri]{nv_lower_tri()}}, \code{\link[=nv_triu]{nv_triu()}}, \code{\link[=prim_iota]{prim_iota()}} for the underlying primitive.
}
