% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_cummin}
\alias{nv_cummin}
\alias{cummin.AnvlArray}
\title{Cumulative Minimum}
\usage{
nv_cummin(x, axis = NULL, indices = FALSE, nan_rm = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array.}

\item{axis}{(\code{integer(1)} | \code{NULL})\cr
Axis along which to accumulate. Negative values count from the end,
i.e. \code{-1} refers to the last axis. If \code{NULL} (default), uses the last
axis.}

\item{indices}{(\code{logical(1)})\cr
If \code{FALSE} (default), returns the running-minimum array.
If \code{TRUE}, returns \code{list(values = ..., indices = ...)} where \code{indices}
is the index of the last occurrence of the running
minimum at each position, of the default integer data
type (see \code{\link[=default_dtypes]{default_dtypes()}}).
When \code{axis = NULL}, indices refer to the flattened input.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in floating-point inputs. If \code{FALSE}
(default), \code{NaN} propagates forward from its first occurrence. If
\code{TRUE}, \code{NaN} is treated as the identity element of the cumulative
op (\code{0} for sum, \code{1} for prod, \code{-Inf} / \code{+Inf} for max / min) and
contributes nothing to the running value.}
}
\value{
\code{\link{arrayish}} (when \code{indices = FALSE}) or named list of two
arrays (when \code{indices = TRUE}).
}
\description{
Running minimum along a single axis, the last one by default.
}
\section{Relation to base R}{

\code{\link[base:cummin]{base::cummin()}} flattens a multi-axis array before
accumulating, while \code{nv_cummin()} (and \code{cummin()} on
an anvl array) accumulates along a single axis, the last one by default.
Flatten with \code{\link[=nv_flatten]{nv_flatten()}} first if you want a single running sequence.
The two orders still differ: anvl arrays are row-major (C order), so the
flattened sequence iterates the last axis fastest, whereas base R uses
column-major (Fortran) order. They agree on 1-D inputs.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(3, 1, 4, 1, 5, 9), nrow = 2)
nv_cummin(x)
nv_cummin(x, axis = 1L)
nv_cummin(x, axis = 1L, indices = TRUE)
nv_cummin(nv_array(c(3, NaN, 1)))                # NaN propagates
nv_cummin(nv_array(c(3, NaN, 1)), nan_rm = TRUE) # NaN skipped
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_cummin]{prim_cummin()}} for the underlying primitive.
}
