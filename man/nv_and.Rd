% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_and}
\alias{nv_and}
\alias{&.AnvlArray}
\title{Bitwise And}
\usage{
nv_and(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Left and right operand.
Operands are \link[=nv_promote_to_common]{promoted to a common data type}.
Scalars are \link[=nv_broadcast_scalars]{broadcast} to the shape of the other operand.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape and the promoted common data type of the inputs.
}
\description{
Element-wise bitwise AND of two integer arrays, which for a boolean array
is the logical AND.

This is \emph{not} what the \code{&} operator does: like in base R, \code{&} is logical,
so it requires a boolean operand and returns a boolean array (see the
section below).
}
\section{The \code{&} operator}{

\code{&} is \emph{logical}, like in base R: it requires boolean operands and returns a
boolean array, which is what \code{nv_and()} computes for them. Unlike base R it
does not coerce a non-boolean operand by comparing it against zero -- write
\code{x != 0} yourself -- because {anvl} does not apply R's data type coercions
anywhere else either.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_and(nv_array(c(TRUE, FALSE, TRUE)), nv_array(c(TRUE, TRUE, FALSE)))
nv_and(nv_array(12L), nv_array(10L)) # bitwise: 8
nv_array(c(TRUE, FALSE)) & nv_array(c(TRUE, TRUE)) # logical
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_and]{prim_and()}} for the underlying primitive.
}
