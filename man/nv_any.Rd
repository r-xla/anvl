% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_any}
\alias{nv_any}
\alias{any.AnvlArray}
\title{Any Reduction}
\usage{
nv_any(x, axes = NULL, drop = TRUE)

\method{any}{AnvlArray}(..., na.rm = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Must be a boolean or an R logical.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reduce over. Negative values count from the end, i.e. \code{-1} refers
to the last axis. If \code{NULL} (default), reduces over all axes.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced axes: removed from the output shape if \code{TRUE},
set to 1 if \code{FALSE}.}

\item{...}{(\code{\link{arrayish}})\cr
The arrays to reduce, and, when there is only one, further named arguments
of \code{\link[=nv_any]{nv_any()}}, such as \code{axes}.}

\item{na.rm}{(\code{logical(1)})\cr
Only used for R values among \code{...}, which are reduced by base R, as a
boolean array has no missing values.}
}
\value{
(\code{\link{arrayish}})\cr
Has the boolean data type. The shape is the input's with the reduced axes
removed (\code{drop = TRUE}) or set to 1 (\code{drop = FALSE}).
}
\description{
Performs logical OR along the specified axes.
Returns \code{TRUE} if any element is \code{TRUE}.
}
\section{The \code{any()} generic}{

\code{any()} reduces over all axes and, like \code{\link[base:any]{base::any()}}, takes several data
arguments: \code{any(x, y)} asks about both arrays. It is \emph{logical}, so -- unlike
base R -- a non-boolean argument is an error rather than a comparison
against zero. Beyond base R, named arguments are passed on, so
\code{any(x, axes = 1L)} reduces a single axis -- but only when \code{x} is the only
data argument.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(TRUE, FALSE, TRUE, TRUE), nrow = 2)
# no axes given: reduce over all of them
nv_any(x)

# reducing axis 1 removes it, drop = FALSE keeps it at size 1
nv_any(x, axes = 1L)
nv_any(x, axes = 1L, drop = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_any]{prim_any()}} for the underlying primitive.
}
