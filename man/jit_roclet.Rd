% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jit-roclet.R
\name{jit_roclet}
\alias{jit_roclet}
\title{\verb{@jit} roxygen roclet}
\usage{
jit_roclet()
}
\value{
A roxygen2 roclet object.
}
\description{
A custom roxygen2 roclet that scans for \verb{@jit} tags on function definitions
and emits a registry of functions to wrap in \code{\link[=jit]{jit()}} at package build
time.

Mark a function with \verb{#' @jit} (optionally \verb{#' @jit static = c(...)}) to
request that the function be replaced in the package namespace by
\code{jit(f, static = c(...))}.
}
\section{Setting up the roclet in a package}{

\enumerate{
\item Add \code{anvl} to your package's \code{Imports}.
\item Activate the roclet in the \code{Roxygen} field of \code{DESCRIPTION}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Roxygen: list(markdown = TRUE,
              roclets = c("namespace", "rd", "anvl::jit_roclet"))
}\if{html}{\out{</div>}}
\item Add \code{R/jit-registry.R} to the \code{Collate} field, \strong{before} \code{R/zzz.R}.
\code{devtools::document()} will (re)generate this file on every run.
\item In \code{R/zzz.R}, apply the registry at the top level (right next to
\code{.onLoad}), so the wrapped functions are byte-compiled during install:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{.onLoad <- function(libname, pkgname) \{ ... \}

anvl::apply_jit_registry(.jit_registry)
}\if{html}{\out{</div>}}
\item Tag the functions you want jitted:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{#' @export
#' @jit static = c("flag")
my_fun <- function(x, flag) if (flag) x + 1 else x * 2
}\if{html}{\out{</div>}}
\item Run \code{devtools::document()}. The first run requires that the previous
install of your package already exports the roclet's pieces; on a fresh
setup, omit the custom roclet from \code{Roxygen}, run \code{document()} once,
install, and then re-enable it. Subsequent runs work as normal.
}
}

\section{Tag syntax}{

\itemize{
\item \verb{#' @jit} — no static arguments.
\item \verb{#' @jit static = c("a", "b")} — names of formals to treat as static.
\item \verb{#' @jit static = c(1L, 2L)} — positions of formals to treat as static.
\item \verb{#' @jit static = 2:5} — the value is evaluated as R, so ranges and any
other expression yielding a character/integer vector are allowed.
\item \verb{#' @jit static 2:5} — terse form; the \code{=} may be omitted.
}

Anything other than a \code{static = ...} argument is rejected.
}

\seealso{
\code{\link[=apply_jit_registry]{apply_jit_registry()}}, \code{\link[=jit]{jit()}}
}
