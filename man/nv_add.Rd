% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_add}
\alias{nv_add}
\title{Addition}
\usage{
nv_add(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Two inputs with a \link[=common_dtype]{common data type}. Can be
any data type. Scalars are broadcast, and R values assume the other
operand's data type within their \link[=dtypes]{data type category},
otherwise falling back to their \link[=default_dtypes]{default data type} and
being converted to the common data type.}
}
\value{
\code{\link{arrayish}}\cr
Has the inputs' broadcast shape and common data type.
}
\description{
Adds two arrays element-wise, after promoting to a common data type and broadcasting scalars.
You can also use the \code{+} operator.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1, 2, 3))
y <- nv_array(c(4, 5, 6))
x + y

# different data types are promoted to their common one
nv_add(nv_scalar(1, "f32"), nv_scalar(2, "f64"))

# a scalar is broadcast, and an R integer joins a float data type
x + 1L
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_add]{prim_add()}} for the underlying primitive.
}
