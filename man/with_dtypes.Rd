% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/default-dtypes.R
\name{with_dtypes}
\alias{with_dtypes}
\title{Run a Function at Given Data Types}
\usage{
with_dtypes(f, dtypes)
}
\arguments{
\item{f}{(\code{function})\cr
The function to wrap.}

\item{dtypes}{(named \code{character()} | named \code{list()})\cr
A mapping of the data type categories (\code{float}, \code{int} and \code{uint}) to data
types, e.g. \code{c(float = "f64", int = "i64")}. Each may be a string or a
\code{\link[tengen:DataType]{DataType}}. A category it does not name is left as it is.}
}
\value{
A \code{function} with the same arguments as \code{f}, a \code{JitFunction} if \code{f}
was one.
}
\description{
\code{with_dtypes()} wraps \code{f} into a function that works at the data types
\code{dtypes} names: on each call every array argument of a category \code{dtypes}
names is converted to that data type, the defaults (see
\code{\link[=default_dtypes]{default_dtypes()}}) are set to the \code{float} / \code{int} entries for the
duration of the call, and every returned array of a named category is
converted as well.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{nv_add_f64 <- with_dtypes(nv_add, c(float = "f64"))
}\if{html}{\out{</div>}}

A category \code{dtypes} does not name is left alone, in the arguments, in the
body and in the result.
}
\details{
Note that \code{f} itself can also change the default data types, which overrides the
defaults configured by \code{with_dtypes()}.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
add_f64 <- with_dtypes(nv_add, c(float = "f64"))
# An `f32` argument is converted, and the result comes back as `f64`
dtype(add_f64(nv_array(1, dtype = "f32"), 2.5))
# A category that is not named is untouched
dtype(add_f64(nv_array(1L, dtype = "i32"), 2L))
# `uint` is converted too, but sets no default
dtype(with_dtypes(nv_add, c(uint = "ui32"))(nv_array(1L, dtype = "ui8"), 2L))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=default_dtypes]{default_dtypes()}}, \code{\link[=local_default_dtypes]{local_default_dtypes()}}
}
