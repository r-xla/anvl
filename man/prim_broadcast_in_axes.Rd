% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_broadcast_in_axes}
\alias{prim_broadcast_in_axes}
\title{Primitive Broadcast}
\usage{
prim_broadcast_in_axes(x, shape, broadcast_axes)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Arrayish value of any data type.}

\item{shape}{(\code{integer()})\cr
Target shape. The size of each mapped axis must either match the size of
the corresponding axis of \code{x}, or that axis of \code{x} must have size 1.}

\item{broadcast_axes}{(\code{integer()})\cr
Maps each axis of \code{x} to an axis of the output.
Must have length equal to the number of axes of \code{x}.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input and the given \code{shape}.
It is ambiguous if the input is ambiguous.
}
\description{
Broadcasts an array to a new shape by replicating the data along new or size-1 axes.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_broadcast_in_dim]{hlo_broadcast_in_dim()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1, 2, 3))
prim_broadcast_in_axes(x, shape = c(2, 3), broadcast_axes = 2L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_broadcast_to]{nv_broadcast_to()}}
}
