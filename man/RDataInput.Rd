% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{RDataInput}
\alias{RDataInput}
\title{R Data Input Class}
\usage{
RDataInput(dtype, shape, r_type)
}
\arguments{
\item{dtype}{(\code{\link[tengen:DataType]{tengen::DataType}} | \code{character(1)})\cr
The dtype the R value is uploaded at.}

\item{shape}{(\code{\link[stablehlo:Shape]{stablehlo::Shape}} | \code{integer()})\cr
The shape of the value.}

\item{r_type}{(\code{character(1)})\cr
The R storage type the caller passes: \code{"double"}, \code{"integer"} or
\code{"logical"}.}
}
\value{
(\code{RDataInput})
}
\description{
The \code{\link{AbstractArray}} of a program input that the caller supplies as bare R
data. Unlike every other input it has no data type of its own to be supplied
at: the program decides one (see \code{\link{RData}}), and the runtime uploads the
R value at that dtype rather than at the default for its R storage type.

It is the resolved form of an \code{\link{RData}}: an \code{RData} is a value whose dtype
is still open, an \code{RDataInput} is the same value once the finished trace has
settled which dtype its input is supplied at. Those are the only two states
an R value needs an aval for. A typed value needs no such pair: an input
whose value is unknown is an \code{\link{AbstractArray}}, and one that carries its
value is a \code{\link{ConcreteArray}}, \code{\link{LiteralArray}} or \code{\link{IotaArray}} -- all of
which already have a dtype.

It does not resolve to a plain \code{\link{AbstractArray}}, because the two say
different things about the caller. \verb{AbstractArray(f64, [2x3])} on an input
means the caller hands over something that already \emph{is} an \verb{f64[2x3]} -- an
array, passed through as it is. \verb{RDataInput(f64, [2x3], "double")} means the
caller hands over an R \code{double}, which the runtime \emph{uploads} at \code{f64} instead
of at the default for its R storage type -- that is what keeps \code{x_f64 / sqrt(2)} exact. Collapsing the two would lose the mark of which arguments
need uploading at a chosen dtype, which is what makes an \code{\link{AnvlGraph}}
self-describing about its inputs: \code{graph_input_dtypes()} reads an upload
dtype off each R-data input and \code{NA} off each array one, and both backends
dispatch on that. The \code{r_type} field records which R storage type arrives,
and so pins down the category the settled dtype had to stay within.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# An f64 program consuming an R double argument uploads it as f64.
graph <- trace_fn(function(x) nv_scalar(1, dtype = "f64") + x, list(x = nv_aval("double", integer()))) # nolint
graph$inputs[[1]]$aval
\dontshow{\}) # examplesIf}
}
\seealso{
\link{RData}, \link{AbstractArray}
}
