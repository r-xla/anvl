% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_polygamma}
\alias{nv_polygamma}
\title{Polygamma}
\usage{
nv_polygamma(n, x)
}
\arguments{
\item{n, x}{(\code{\link{arrayish}})\cr
Order of the polygamma function and the value to evaluate it at. \code{n}
typically holds non-negative whole numbers. Can be of any data type: the
two are
\link[=nv_promote_to_common]{promoted to a common data type}, which is a float
because that is all \code{\link[=prim_polygamma]{prim_polygamma()}} takes, so an integer or boolean
input is converted rather than refused. Scalars are
\link[=nv_broadcast_scalars]{broadcast} to the shape of the other, so
\code{nv_polygamma(1, x)} works for any \code{x}.}
}
\value{
(\code{\link{arrayish}})\cr
Has the inputs' broadcast shape and common data type.
}
\description{
Element-wise polygamma function: the \code{(n+1)}-th derivative of the
log-gamma function. The order \code{n} is broadcast against \code{x} (so
\code{nv_polygamma(1, x)} works for any \code{x}). For \code{n = 0} this is the
digamma function; for \code{n = 1}, \code{trigamma()} dispatches here.

Inputs are
\link[=nv_promote_to_common]{promoted to a common floating data type} and
scalar arguments are
\link[=nv_broadcast_scalars]{broadcast} to the shape of the non-scalar
arguments.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(0.5, 1, 2, 5))
nv_polygamma(1, x) # trigamma
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_polygamma]{prim_polygamma()}} for the underlying primitive.
}
