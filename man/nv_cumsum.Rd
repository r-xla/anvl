% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_cumsum}
\alias{nv_cumsum}
\alias{cumsum.AnvlArray}
\title{Cumulative Sum}
\usage{
nv_cumsum(x, axis = NULL, nan_rm = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value materializes at its
\link[=default_dtypes]{default data type}.}

\item{axis}{(\code{integer(1)} | \code{NULL})\cr
Axis along which to accumulate. Negative values count from the end,
i.e. \code{-1} refers to the last axis. If \code{NULL} (default), the input
is first flattened to a 1-D array, like \code{\link[base:cumsum]{base::cumsum()}}.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in float inputs. If \code{FALSE}
(default), \code{NaN} propagates forward from its first occurrence. If
\code{TRUE}, \code{NaN} is treated as the identity element of the cumulative
op (\code{0} for sum, \code{1} for prod, \code{-Inf} / \code{+Inf} for max / min) and
contributes nothing to the running value.}
}
\value{
(\code{\link{arrayish}})\cr
Has the input's shape when \code{axis} is given, and is 1-D of length
\code{prod(shape(x))} when \code{axis} is \code{NULL}, which flattens first. Has the
input's data type, except for a boolean input, which is accumulated at the
default integer data type (see \code{\link[=default_dtypes]{default_dtypes()}}).
}
\description{
Cumulative sum, optionally along a single axis.
A boolean array is counted, like \code{\link[base:cumsum]{base::cumsum()}} does.
}
\section{Relation to base R}{

\code{nv_cumsum()} with \code{axis = NULL} and \code{\link[base:cumsum]{base::cumsum()}}
both flatten first, but in different orders -- anvl arrays are row-major (C
order), base R is column-major (Fortran) -- so for a multi-axis input the
two give different running values. They agree on 1-D inputs.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(1:6, nrow = 2)
nv_cumsum(x)              # row-major flatten, then accumulate
nv_cumsum(x, axis = 1L)    # accumulate along rows
nv_cumsum(nv_array(c(1, NaN, 3)))                # NaN propagates
nv_cumsum(nv_array(c(1, NaN, 3)), nan_rm = TRUE) # NaN treated as 0
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_cumsum]{prim_cumsum()}} for the underlying primitive.
}
