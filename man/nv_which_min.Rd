% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_which_min}
\alias{nv_which_min}
\title{Index of the Minimum}
\usage{
nv_which_min(x, axes = NULL, drop = TRUE, nan_rm = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value materializes at its
\link[=default_dtypes]{default data type}.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reduce over. Negative values count from the end, i.e. \code{-1} refers
to the last axis. If \code{NULL} (default), reduces over all axes.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced axes: removed from the output shape if \code{TRUE},
set to 1 if \code{FALSE}.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in float inputs. If \code{FALSE}
(default), \code{NaN} propagates. If \code{TRUE}, \code{NaN} values are skipped.}
}
\value{
(\code{\link{arrayish}})\cr
Has the default integer data type (see \code{\link[=default_dtypes]{default_dtypes()}}) regardless of
the input's, and the input's shape with \code{axes} removed (\code{drop = TRUE}) or
set to 1 (\code{drop = FALSE}).
}
\description{
Returns the index of the minimum value over one or more axes. Ties are
broken by returning the smallest index.
}
\section{Reducing several axes}{

\code{nv_which_min()} is the index to \code{\link[=nv_min]{nv_min()}}'s value: called with the
same \code{axes} and \code{drop}, it points at the element whose value
\code{nv_min()} returns. Reducing several axes ranks their elements
together, and the result indexes the column-major flattening of those axes
-- the order \code{\link[=nv_flatten]{nv_flatten()}} produces and \code{\link[base:which.min]{base::which.min()}} reports -- which
is also the order ties are broken in.
}

\section{NaN handling}{

With \code{nan_rm = FALSE} (default), if any entry being reduced is \code{NaN}, the
returned index points at the first such \code{NaN}. With \code{nan_rm = TRUE}, \code{NaN}
entries are skipped.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# the index comes out at the default integer data type
nv_which_min(nv_array(c(3, 1, 4, 1, 5, 9, 2, 6)))
m <- nv_matrix(c(3, 1, 5, 2, 4, 0), nrow = 2, byrow = TRUE)
nv_which_min(m) # indexes the flattened matrix
nv_which_min(m, axes = 2L) # one index per row
nv_which_min(nv_array(c(2, NaN, 1, 3)))
nv_which_min(nv_array(c(2, NaN, 1, 3)), nan_rm = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_which_max]{nv_which_max()}}, \code{\link[=nv_min]{nv_min()}}.
}
