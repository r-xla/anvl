% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_polygamma}
\alias{prim_polygamma}
\title{Primitive Polygamma}
\usage{
prim_polygamma(n, x)
}
\arguments{
\item{n, x}{(\code{\link{arrayish}})\cr
Arrayish values of data type floating-point.
Must have the same shape.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape as the operands, and the data type they were brought
to: the one an operand already had, or -- if all of them are R values --
the default of their R storage type.
}
\description{
Element-wise polygamma function: the \code{(n+1)}-th derivative of the
log-gamma function. Both \code{n} and \code{x} must have the same shape; \code{n}
typically holds non-negative integer values.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_polygamma]{hlo_polygamma()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
n <- nv_array(c(1, 1, 2))
x <- nv_array(c(0.5, 1, 2))
prim_polygamma(n, x)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_polygamma]{nv_polygamma()}}
}
