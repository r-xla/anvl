% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_reshape}
\alias{prim_reshape}
\title{Primitive Reshape}
\usage{
prim_reshape(x, shape)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value commits to its
\link[=default_dtypes]{default data type}.}

\item{shape}{(\code{integer()})\cr
Target shape. Must have the same number of elements as \code{x}.
At most one entry may be \code{-1}, in which case its extent is inferred from
the remaining entries and the number of elements of \code{x}.}
}
\value{
(\code{\link{arrayish}})\cr
Has the same data type as the input and the given \code{shape}.
}
\description{
Reshapes an array to a new shape without changing the underlying data.
Note that row-major order is used, which differs from R's column-major order.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_reshape]{hlo_reshape()}}, specified under \href{https://openxla.org/stablehlo/spec#reshape}{reshape}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# the elements are reread in row-major order; the data type is untouched
x <- nv_array(1:6)
prim_reshape(x, shape = c(2, 3))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_reshape]{nv_reshape()}}
}
