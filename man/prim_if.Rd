% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_if}
\alias{prim_if}
\title{Primitive If}
\usage{
prim_if(pred, true, false)
}
\arguments{
\item{pred}{(\code{\link{arrayish}})\cr
Predicate deciding which branch to execute. Must be a scalar of the
boolean data type, or an R logical.}

\item{true, false}{(\verb{function()})\cr
Zero-argument functions for the true and false branches. Both must return
outputs of the same structure, data types and shapes. As with
\code{\link[=prim_ifelse]{prim_ifelse()}}, whose two values must already agree, nothing is promoted:
branches that disagree are an error. They take no arguments and reach the
values they use by closing over them; those values are recorded as
operands of the call, so a gradient flows back through them.}
}
\value{
(\code{\link{arrayish}} | \code{list})\cr
Result of the executed branch: an array, or a tree of them in the sense
of pjrt's \code{\link[pjrt:build_tree]{RTree}} -- a \code{list}, nested arbitrarily -- with
the structure, data types and shapes both branches share.
}
\description{
Conditional execution of one of two branches based on a scalar boolean
predicate, mirroring R's \code{if} construct: it branches between two
\emph{functions} and evaluates only the selected one. Its arguments are named
after that construct, where \code{\link[=prim_ifelse]{prim_ifelse()}} -- which selects
element-wise between two \emph{arrays} -- is named after \code{\link[=ifelse]{ifelse()}}.
}
\section{Gradients}{

The backward pass is itself a \code{\link[=prim_if]{prim_if()}} on the same predicate, so only the
taken branch's gradient is computed. A value that only the other branch uses
gets a zero.
}

\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_if]{hlo_if()}}, specified under
\href{https://openxla.org/stablehlo/spec#if}{if}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# both branches must return the same structure, data types and shapes
prim_if(nv_scalar(TRUE), \() nv_scalar(1), \() nv_scalar(2))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_if]{nv_if()}}, \code{\link[=prim_ifelse]{prim_ifelse()}}
}
