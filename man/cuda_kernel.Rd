% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{cuda_kernel}
\alias{cuda_kernel}
\title{CUDA Kernel Launch}
\usage{
cuda_kernel(module, kernel, grid, block, scalars = list(), shared_mem = 0L)
}
\arguments{
\item{module}{(\code{PJRTCudaModule})\cr
The module containing the kernel, from \code{\link[pjrt:pjrt_cuda_module]{pjrt::pjrt_cuda_module()}}.}

\item{kernel}{(\code{character(1)})\cr
The kernel's name, or for a template one of the module's \code{kernels}
expressions, e.g. \code{"scale<float>"}.}

\item{grid, block}{(\code{integer()})\cr
Number of blocks, and threads per block, in up to three dimensions. A
grid with a zero dimension launches nothing.}

\item{scalars}{(\code{list()})\cr
Scalar arguments passed after the buffers. An R integer is passed as a
C \code{int}, a double as a \code{double} and a logical as a \code{bool}; use
\code{\link[pjrt:pjrt_cuda_scalar]{pjrt::pjrt_cuda_scalar()}} for other C types.}

\item{shared_mem}{(\code{integer(1)})\cr
Bytes of dynamic shared memory per block.}
}
\value{
\code{AnvlCudaKernel}
}
\description{
Describes a launch of a CUDA kernel, for use as the \code{target} of
\code{\link[=nv_custom_call]{nv_custom_call()}}. This is the way to run a hand-written kernel
without writing an XLA FFI handler: the kernel is compiled from source
the first time it runs (see \code{\link[pjrt:pjrt_cuda_module]{pjrt::pjrt_cuda_module()}}), and {pjrt}'s
built-in \code{pjrt_cuda_kernel} handler launches it.

The kernel receives the call's operands and then its results as device
pointers, followed by \code{scalars}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{__global__ void kernel(const T *in_1, ..., T *out_1, ..., <scalars>)
}\if{html}{\out{</div>}}

The launch configuration is fixed when the call is traced, which is
possible because shapes are static: derive \code{grid} from the operands'
\code{\link[=shape]{shape()}} rather than from their values.
}
\examples{
scale <- pjrt::pjrt_cuda_module(r"(
extern "C" __global__ void scale(const float *x, float *out, float a, int n) {
  int i = blockIdx.x * blockDim.x + threadIdx.x;
  if (i < n) out[i] = a * x[i];
})")
cuda_kernel(
  scale, "scale",
  grid = 4L, block = 256L,
  scalars = list(pjrt::pjrt_cuda_scalar(2, "f32"), 1000L)
)
}
\seealso{
\code{\link[=nv_custom_call]{nv_custom_call()}}
}
