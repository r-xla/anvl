% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_triangular_solve}
\alias{nv_triangular_solve}
\title{Triangular Solve}
\usage{
nv_triangular_solve(
  a,
  b,
  left_side = TRUE,
  lower = TRUE,
  unit_diagonal = FALSE,
  transpose_a = FALSE
)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Triangular coefficient matrix with at least 2 axes. The last two
axes must be equal; any leading axes are batch axes.}

\item{b}{(\code{\link{arrayish}})\cr
Right-hand side. For \code{a} of shape \verb{(B..., n, n)}, \code{b} may be either:
\itemize{
\item full rank — shape \verb{(B..., n, k)} when \code{left_side = TRUE}, or
\verb{(B..., k, n)} when \code{left_side = FALSE};
\item one rank less, shape \verb{(B..., n)}, meaning a single column
(\code{left_side = TRUE}) or row (\code{left_side = FALSE}) per batch — it
is reshaped internally and the reshape is undone on the result so
the output rank matches \code{b}.
}

\code{b}'s batch axes (\code{B...}) must match \code{a}'s exactly.}

\item{left_side}{(\code{logical(1)})\cr
If \code{TRUE} (default), solve \code{op(a) \%*\% x = b}; if \code{FALSE},
solve \code{x \%*\% op(a) = b}.}

\item{lower}{(\code{logical(1)})\cr
Whether \code{a} is lower or upper triangular. Defaults to \code{TRUE}.}

\item{unit_diagonal}{(\code{logical(1)})\cr
If \code{TRUE}, the diagonal of \code{a} is treated as all ones (and the actual
values on the diagonal are ignored). Defaults to \code{FALSE}.}

\item{transpose_a}{(\code{logical(1)})\cr
If \code{TRUE}, solve with \code{t(a)} in place of \code{a}. Defaults to \code{FALSE}.}
}
\value{
\code{\link{arrayish}}\cr
The solution \code{x}, with the same shape and dtype as \code{b}.
}
\description{
Solves a triangular system of linear equations. When \code{left_side = TRUE},
returns \code{x} such that \code{op(a) \%*\% x = b}. When \code{left_side = FALSE},
returns \code{x} such that \code{x \%*\% op(a) = b}. Here \code{op} is \code{a} or \code{t(a)}
depending on \code{transpose_a}.
}
\details{
As a convenience, \code{b} may have one fewer axis than \code{a} (a single
right-hand side per batch, shape \verb{(B..., n)} for \code{a} of shape
\verb{(B..., n, n)}). It is reshaped internally to a column (\code{left_side = TRUE}) or row (\code{left_side = FALSE}) and reshaped back on the way out.
Because we don't broadcast, this is not ambiguous (as it would be for NumPy).
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
L <- nv_matrix(c(2, 1, 0, 3), nrow = 2, dtype = "f32")
b <- nv_matrix(c(4, 3), nrow = 2, dtype = "f32")
nv_triangular_solve(L, b)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_solve]{nv_solve()}}, \code{\link[=nv_chol]{nv_chol()}}, \code{\link[=prim_triangular_solve]{prim_triangular_solve()}}
}
