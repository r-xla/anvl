% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_cummin}
\alias{prim_cummin}
\title{Primitive Cumulative Minimum}
\usage{
prim_cummin(operand, axis)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Arrayish value of any data type.}

\item{axis}{(\code{integer(1)})\cr
Axis along which to accumulate.}
}
\value{
\code{list} of two \code{\link{arrayish}} values:\cr
The running minimum (same dtype as \code{operand}) and the
running argmin (dtype \code{i32}, 1-based). Both have
the same shape as \code{operand}.
}
\description{
Running minimum of array elements along a single axis along with
the index of the last occurrence of the running minimum.
At output position \code{j}, the values output is \code{min(input[1:j])} and the
indices output is the largest \code{i} in \code{1:j} with
\code{input[i] == values[j]} (last-occurrence tiebreak).
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to a variadic \code{\link[stablehlo:hlo_reduce_window]{hlo_reduce_window()}} over \verb{(values, iota)}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(3, 1, 4, 1, 5, 9), nrow = 2)
prim_cummin(x, axis = 1L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_cummin]{nv_cummin()}}
}
