% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/promotion.R
\name{apply_promotion}
\alias{apply_promotion}
\title{Bring a Primitive's Operands to One Data Type}
\usage{
apply_promotion(operands, promote)
}
\arguments{
\item{operands}{(\code{list()})\cr
The operands, named as the primitive's \code{\link[=graph_desc_add]{graph_desc_add()}} call names them.}

\item{promote}{(\code{function})\cr
The rule to apply; see \link{promotion_rule}.}
}
\value{
(\code{list()})\cr
\code{operands}, each realized at the data type the rule named for it.
}
\description{
Applies a \link[=promotion_rule]{promotion rule} to the operands of a primitive,
inside the primitive's own body.

A primitive promotes nothing on its own: an R value among its operands would
commit to its own default, so whether a call worked would depend on whether
the array it met happened to be at that default. A primitive whose operands
must \emph{agree} says so with this, on the same list it goes on to hand
\code{\link[=graph_desc_add]{graph_desc_add()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{function(lhs, rhs) \{
  operands <- apply_promotion(list(lhs = lhs, rhs = rhs), promote_rdata_common())
  graph_desc_add(self, operands, infer_fn = infer_fn)[[1L]]
\}
}\if{html}{\out{</div>}}

\code{\link[=promote_rdata_common]{promote_rdata_common()}} is the rule for it: an operand that has a data type
keeps it, and an R value takes the one the others have, within its own category.
That is what makes \code{prim_mul(x_f64, 2)} work whatever \code{x}'s data type is,
while keeping a primitive from widening the array it was handed -- promotion
across categories is the \verb{nv_*} layer's job.
}
\details{
Pass only the operands that must agree, and name them as the
\code{\link[=graph_desc_add]{graph_desc_add()}} call names them. \code{\link[=prim_ifelse]{prim_ifelse()}} promotes its two
branches and leaves \code{pred} a \code{bool}; \code{\link[=prim_scatter]{prim_scatter()}} promotes \code{x} and
\code{update} and leaves the indices alone. A primitive with one arrayish operand,
or with deliberately heterogeneous ones (\code{\link[=prim_sort]{prim_sort()}}'s payload,
\code{\link[=prim_while]{prim_while()}}'s loop state), calls this not at all.

Call it before the body uses the operands for anything else, so it sees
settled data types throughout: \code{\link[=prim_reduce]{prim_reduce()}} reads \code{dtype(init)} to trace
its reductor and \code{\link[=prim_scatter]{prim_scatter()}} builds its update computation's parameter
slots from \code{\link[=peek_dtype]{peek_dtype()}}, both before recording a call.

It is idempotent: once every operand is at the data type the rule names,
realizing them again changes nothing.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# An R value takes the data type of the operand it meets.
operands <- apply_promotion(list(lhs = nv_scalar(1, "f64"), rhs = 2), promote_rdata_common())
dtype(operands$rhs)
\dontshow{\}) # examplesIf}
}
\seealso{
\link{promotion_rule}, \code{\link[=new_primitive]{new_primitive()}}, \code{vignette("extending_primitive")}
}
