% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_qr}
\alias{prim_qr}
\title{Primitive QR Decomposition}
\usage{
prim_qr(x)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input, with exactly 2 axes. Can be any float data type. An R value commits to its
\link[=default_dtypes]{default data type}.}
}
\value{
(named \code{list} of two \code{\link{arrayish}})\cr
Elements \code{Q} (shape \verb{(m, k)}) and \code{R}
(shape \verb{(k, n)}), where \verb{(m, n) = shape(x)} and
\code{k = min(m, n)}. Both have the input's data type.
}
\description{
Computes the reduced QR decomposition of a matrix \code{x}:
\deqn{A = Q R,}
where \eqn{Q} has orthonormal columns (\eqn{Q^\top Q = I}) and
\eqn{R} is upper triangular.
For an \eqn{m \times n} input with \eqn{k = \min(m, n)}, \eqn{Q} has
shape \eqn{m \times k} and \eqn{R} has shape \eqn{k \times n}.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
}
}

\section{StableHLO}{

Lowers to a \code{"geqrf"} + \code{"orgqr"} \code{\link[stablehlo:hlo_custom_call]{hlo_custom_call()}} pair
(backed by LAPACK on CPU and cuSOLVER on CUDA) + postprocessing.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(1:6, shape = c(3, 2), dtype = "f32")
prim_qr(x)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_qr]{nv_qr()}}
}
