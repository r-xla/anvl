% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_mod}
\alias{nv_mod}
\title{Modulo (Flooring Remainder)}
\usage{
nv_mod(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Left and right operand.
Operands are \link[=nv_promote_to_common]{promoted to a common data type}.
Scalars are \link[=nv_broadcast_scalars]{broadcast} to the shape of the other operand.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape and the promoted common data type of the inputs.
}
\description{
Element-wise flooring remainder of division. The sign of the result equals
the sign of \code{rhs}, matching base R's \code{\%\%} operator.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1L, -1L))
y <- nv_array(c(-3L, 3L))
nv_mod(x, y)
as.vector(x) \%\% as.vector(y)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_remainder]{nv_remainder()}} for truncating remainder, \code{\link[=nv_int_div]{nv_int_div()}} for the
matching division, \code{\link[=prim_remainder]{prim_remainder()}} for the underlying primitive.
}
