% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_mod}
\alias{nv_mod}
\title{Modulo (Flooring Remainder)}
\usage{
nv_mod(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Two inputs with a \link[=common_dtype]{common data type}. Can be
any numeric data type. Scalars are broadcast, and R values assume the other
operand's data type within their \link[=dtypes]{data type category},
otherwise falling back to their \link[=default_dtypes]{default data type} and
being converted to the common data type.}
}
\value{
(\code{\link{arrayish}})\cr
Has the inputs' broadcast shape and common data type.
}
\description{
Element-wise flooring remainder of division. The sign of the result equals the sign of \code{rhs}, matching base R's \code{\%\%}
operator.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1L, -1L))
y <- nv_array(c(-3L, 3L))
nv_mod(x, y)
as.vector(x) \%\% as.vector(y)

# Different data types are promoted to their common one
nv_mod(nv_scalar(1L, "i32"), nv_scalar(-3L, "i64"))

# A scalar is broadcast
x \%\% 2L
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_remainder]{nv_remainder()}} for truncating remainder, \code{\link[=prim_remainder]{prim_remainder()}} for
the underlying primitive.
}
