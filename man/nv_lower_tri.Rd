% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_lower_tri}
\alias{nv_lower_tri}
\title{Lower Triangular Mask}
\usage{
nv_lower_tri(shape, diagonal = -1L, device = NULL)
}
\arguments{
\item{shape}{(\code{integer()})\cr
Shape.}

\item{diagonal}{(\code{integer(1)})\cr
Diagonal offset, with the same meaning as in \code{\link[=nv_tril]{nv_tril()}}. The default
\code{-1} excludes the main diagonal, matching \code{lower.tri()}; use \code{0} to
include it, matching \code{lower.tri(diag = TRUE)}.}

\item{device}{( \code{character(1)} | \code{PJRTDevice} | \code{\link{quickr_device}} | \code{NULL})\cr
Device for data to live on.}
}
\value{
\code{\link{arrayish}}\cr
Has the given \code{shape} and dtype \code{bool}.
}
\description{
Returns a boolean matrix that is \code{TRUE} on and below the given diagonal,
mirroring base R's \code{lower.tri()}. Use \code{\link[=nv_tril]{nv_tril()}} to zero out the other
triangle of an existing array instead.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
nv_lower_tri(c(3, 3))
nv_lower_tri(c(3, 3), diagonal = 0L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_upper_tri]{nv_upper_tri()}}, \code{\link[=nv_tril]{nv_tril()}}, \code{\link[=prim_iota]{prim_iota()}} for the underlying primitive.
}
