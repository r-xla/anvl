% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-distributions.R
\name{nv_uniform}
\alias{nv_uniform}
\alias{nv_dunif}
\alias{nv_punif}
\alias{nv_qunif}
\title{The Uniform Distribution}
\usage{
nv_dunif(x, min = 0, max = 1, log = FALSE)

nv_punif(q, min = 0, max = 1, lower_tail = TRUE, log_p = FALSE)

nv_qunif(p, min = 0, max = 1, lower_tail = TRUE, log_p = FALSE)
}
\arguments{
\item{x, q}{(\code{\link{arrayish}})\cr
Quantiles at which to evaluate the density (\code{x}) or the distribution
function (\code{q}).}

\item{min, max}{(\code{\link{arrayish}})\cr
Lower and upper limits of the distribution. Either scalars, or arrays of
exactly the same shape as \code{x}/\code{q}/\code{p}, in which case the interval varies
elementwise and each element of \code{x}/\code{q}/\code{p} is evaluated against its own
\code{min}/\code{max}.}

\item{log, log_p}{(\code{logical(1)})\cr
If \code{TRUE}, the densities/probabilities are given as logarithms. For
\code{nv_qunif} this describes the input \code{p}.}

\item{lower_tail}{(\code{logical(1)})\cr
If \code{TRUE} (default), probabilities are \eqn{P(X \le x)}; otherwise,
\eqn{P(X > x)}.}

\item{p}{(\code{\link{arrayish}})\cr
Probabilities at which to evaluate the quantile function. Values outside
\eqn{[0, 1]} give \code{NaN}.}
}
\value{
\code{nv_dunif()}, \code{nv_punif()}, and \code{nv_qunif()} return an \code{\link{arrayish}} with the
same shape and data type as \code{x}/\code{q}/\code{p}.
}
\description{
Density (\code{nv_dunif}), distribution function (\code{nv_punif}), and quantile
function (\code{nv_qunif}) for the Uniform distribution on the interval from
\code{min} to \code{max}.
}
\details{
The Uniform distribution has probability density function:
\deqn{f(x) = \frac{1}{b - a}, \quad a \le x \le b}
and zero elsewhere, where \eqn{a} is \code{min} and \eqn{b} is \code{max}.
The \code{min} and \code{max} are converted to the data type of \code{x}/\code{q}/\code{p}.

All three are univariate functions evaluated elementwise, returning one
value per element of \code{x}/\code{q}/\code{p}. Non-scalar \code{min}/\code{max} therefore give a
separate univariate Uniform per element, \emph{not} a multivariate Uniform over
the hyper-rectangle \eqn{\prod_i [a_i, b_i]}. For that, reduce over the
result: \code{nv_reduce_prod(nv_dunif(x, min, max))}, or
\code{nv_reduce_sum(nv_dunif(x, min, max, log = TRUE))} on the log scale.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(-0.5, 0, 0.25, 1, 1.5))
nv_dunif(x)
nv_dunif(x, min = -1, max = 2)
nv_dunif(x, log = TRUE)

# `min`/`max` may vary elementwise, giving one univariate Uniform per
# element rather than a single distribution over a hyper-rectangle
lower <- nv_array(c(-1, -1, 0, 0, 1))
upper <- nv_array(c(0, 1, 1, 2, 2))
nv_dunif(x, min = lower, max = upper)

nv_punif(x)
nv_punif(x, min = -1, max = 2)
nv_punif(x, lower_tail = FALSE)
nv_punif(x, log_p = TRUE)

p <- nv_array(c(0.025, 0.5, 0.975))
nv_qunif(p)
nv_qunif(p, min = -1, max = 2)
nv_qunif(p, lower_tail = FALSE)
nv_qunif(nv_array(c(-700, -2, -0.1), dtype = "f64"), log_p = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_runif]{nv_runif()}} for sampling from a uniform distribution.
}
