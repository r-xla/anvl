% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_aperm}
\alias{nv_aperm}
\alias{nv_transpose}
\alias{t.AnvlArray}
\alias{aperm.AnvlArray}
\title{Transpose}
\usage{
nv_aperm(x, perm = NULL)

nv_transpose(x, perm = NULL)

\method{aperm}{AnvlArray}(a, perm = NULL, ...)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value materializes at its
\link[=default_dtypes]{default data type}.}

\item{perm}{(\code{integer()} | \code{NULL})\cr
New ordering of axes. If \code{NULL} (default), reverses the axes.
Negative values count from the end, i.e. \code{-1} refers to the last axis.}

\item{a}{(\code{\link{arrayish}})\cr
The array whose axes to permute.}

\item{...}{No additional arguments.}
}
\value{
(\code{\link{arrayish}})\cr
Has \code{x}'s data type and shape \code{shape(x)[perm]}, or \code{rev(shape(x))}
when \code{perm} is \code{NULL}.
}
\description{
Permutes the axes of an array, like \code{\link[base:aperm]{base::aperm()}}. \code{nv_transpose()} is
another spelling of the same function. You can also use \code{aperm()}, or
\code{t()} for matrices.
}
\section{The \code{t()} generic}{

\code{t()} requires a matrix, whereas \code{\link[base:t]{base::t()}} also transposes a vector (into
a one-row matrix) and reverses the axes of a higher-rank array.
}

\section{The \code{aperm()} generic}{

\code{aperm()} permutes the axes exactly as \code{nv_aperm()} does; like
\code{\link[base:aperm]{base::aperm()}}, \code{perm = NULL} reverses them.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# the 2x3 becomes a 3x2, keeping its data type
x <- nv_matrix(1:6, nrow = 2)
nv_aperm(x)
t(x)
nv_aperm(nv_array(1:24, shape = c(2, 3, 4)), c(3, 1, 2))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_transpose]{prim_transpose()}} for the underlying primitive.
}
