% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_shift_left}
\alias{prim_shift_left}
\title{Primitive Shift Left}
\usage{
prim_shift_left(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Arrayish values of data type boolean, integer, or unsigned integer, of the same shape.

Both operands must have the same data type: one that already has a data
type keeps it -- the primitive converts neither operand to meet the other,
so two different data types are an error. An R value has no data type of
its own and is built at the other operand's, as long as that data type is
in its own category (a double becomes a float, an integer an integer or
unsigned integer, a logical a \code{bool}); two R values must be of one R
storage type and are built at its default data type (\code{f32}, \code{i32} or
\code{bool}).

To combine different data types, convert one with \code{\link[=nv_convert]{nv_convert()}} or use
the \verb{nv_*} layer, which promotes to a common data type. See
\code{vignette("type-promotion")}.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape as the operands, and the data type they were brought
to: the one an operand already had, or -- if all of them are R values --
the default of their R storage type.
}
\description{
Element-wise left bit shift.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_shift_left]{hlo_shift_left()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1L, 2L, 4L))
y <- nv_array(c(1L, 2L, 1L))
prim_shift_left(x, y)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_shift_left]{nv_shift_left()}}
}
