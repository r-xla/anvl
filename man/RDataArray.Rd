% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array.R
\name{RDataArray}
\alias{RDataArray}
\title{R Data Array Class}
\usage{
RDataArray(data, shape, r_type = typeof(data))
}
\arguments{
\item{data}{(\code{NULL} | \code{numeric()} | \code{logical()})\cr
The R data. \code{NULL} for an argument of a jitted function, whose value is
deliberately unknown while tracing: the compiled program is cached by the
argument's R type and shape, so it must not depend on the value.}

\item{shape}{(\code{\link[stablehlo:Shape]{stablehlo::Shape}} | \code{integer()})\cr
The shape of the value: \verb{()} for a length-1 vector, its \code{dim()} for an
R array.}

\item{r_type}{(\code{character(1)})\cr
The R storage type: \code{"double"}, \code{"integer"} or \code{"logical"}. Inferred from
\code{data} when that is given.}
}
\description{
An \code{\link{AbstractArray}} for an R value that entered a program without a data
type: a length-1 vector or an \code{\link[=array]{array()}}, either written in the body of a
traced function (\code{x / sqrt(2)}) or passed as an argument to a jitted one.

Unlike every other abstract array it has \strong{no} data type. R has no dtype to
give -- \code{1.5} is neither an \code{f32} nor an \code{f64} -- so instead of stamping one
at the boundary, the value is kept as R data and built into the program at
each use site, at the dtype that use site turns out to need. \code{x_f64 / sqrt(2)} therefore sees the exact double, rather than one that was rounded
to \code{f32} on the way in and widened again.

Which dtype a use site needs is normally not decided value by value: an
\verb{nv_*} function canonicalizes its whole argument set once at the top with
\code{\link[=as_anvl_arrays]{as_anvl_arrays()}}, and the \link[=promote_rule]{\code{.promote} rule} it passes
there is what names the dtype every R value among them is built at.

A value that is never combined with a typed array has nothing to take its
dtype from and \emph{commits} to the default for its R type: \code{f32} for a double,
\code{i32} for an integer, \code{bool} for a logical.
}
\section{Extractors}{

\code{\link[tengen:shape]{shape()}} and \code{\link[tengen:naxes]{naxes()}} answer as they
would for the R value: \verb{()} for a length-1 vector, \code{dim()} for an
\code{\link[=array]{array()}}, and an error for a longer vector without a \code{dim()}, which is not
an arrayish value at all. \code{\link[tengen:dtype]{dtype()}} \strong{errors}: there is no
dtype to report until the value commits, exactly as \code{dtype(1.5)} has none to
report. Give the value a dtype (e.g. \code{\link[=nv_array]{nv_array()}}, \code{\link[=nv_scalar]{nv_scalar()}}) to ask.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- RDataArray(1.5, shape = integer())
x
shape(x)
# dtype(x) would error: 1.5 has no data type of its own

# How it appears during tracing
graph <- trace_fn(function(x) x, list(x = RDataArray(NULL, integer(), "double")))
graph
\dontshow{\}) # examplesIf}
}
\seealso{
\link{AbstractArray}, \link{LiteralArray}
}
