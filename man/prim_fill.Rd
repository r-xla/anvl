% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_fill}
\alias{prim_fill}
\title{Primitive Fill}
\usage{
prim_fill(value, shape, dtype, device = NULL)
}
\arguments{
\item{value}{(\code{numeric(1)} | \code{logical(1)})\cr
Scalar value to fill the array with, which has to be something \code{dtype} can
hold: a number for a float, a whole number for an integer, a non-negative
whole number for an unsigned integer and a logical for \code{bool}. A
\code{\link[bit64:integer64]{bit64::integer64}} counts as a whole number, and \code{NA} is rejected, since
XLA has no missing value. Whether the value is \emph{in range} is the backend's
business -- \code{prim_fill(300L, dtype = "i8")} still wraps.}

\item{shape}{(\code{integer()})\cr
Shape of the output array.}

\item{dtype}{(\code{character(1)} | \code{\link[tengen:DataType]{DataType}})\cr
Data type of the result. Can be any data type.}

\item{device}{(\code{NULL} | \code{character(1)} | \link[=nv_device]{device})\cr
The device the data lives on, given either as:
\itemize{
\item a \emph{device string} naming the platform (e.g. \code{"cpu"}, \code{"cuda"},
\code{"cuda:<n>"}), which is resolved against the backend in use, or
\item a \emph{device object} as returned by \code{\link[=nv_device]{nv_device()}}: a
\code{\link[pjrt:pjrt_device]{PJRTDevice}} for the \code{"pjrt"} backend or a
\code{\link{quickr_device}} for the \code{"quickr"} backend. Because a device object
is backend-specific, it also determines the backend.
}

The default (\code{NULL}) uses \code{\link[=default_device]{default_device()}}: the CPU, or the platform
named by the \code{PJRT_PLATFORM} environment variable on the \code{"pjrt"} backend.}
}
\value{
(\code{\link{arrayish}})\cr
Has the given \code{shape} and \code{dtype}.
}
\description{
Creates an array of a given shape and data type, filled with a scalar value.
The advantage of using this function instead of e.g. doing
\code{nv_array(1, shape = c(100, 100))} is that lowering of \code{\link[=prim_fill]{prim_fill()}} is
efficiently represented in the compiled program, while the latter uses
100 * 100 * 4 bytes of memory.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_tensor]{hlo_tensor()}}, stablehlo's constant builder, specified under
\href{https://openxla.org/stablehlo/spec#constant}{constant}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# the R double is built at the requested data type
prim_fill(3.14, shape = c(2, 3), dtype = "f32")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_fill]{nv_fill()}}
}
