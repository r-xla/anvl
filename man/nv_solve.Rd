% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_solve}
\alias{nv_solve}
\alias{solve.AnvlArray}
\title{Solve Linear System}
\usage{
nv_solve(a, b)

\method{solve}{AnvlArray}(a, b, ...)
}
\arguments{
\item{a}{(\code{\link{arrayish}})\cr
Square non-singular matrix with exactly 2 axes. Can be any float data
type. \code{a} and \code{b} must have the same data type. An R value among them assumes the data type of the others when it is in its \link[=dtypes]{data type category}, and its \link[=default_dtypes]{default data type} when none of them has one.}

\item{b}{(\code{\link{arrayish}})\cr
Right-hand side, vector of length \code{n} or matrix with \code{n} rows.}

\item{...}{No additional arguments.}
}
\value{
(\code{\link{arrayish}})\cr
The solution \code{x} such that \code{a \%*\% x = b}, with \code{b}'s shape and the data
type \code{a} and \code{b} agreed on.
}
\description{
Solves the linear system \code{a \%*\% x = b} for \code{x}. Uses LU decomposition
with partial pivoting internally, so \code{a} need only be square and
non-singular.
}
\details{
\deqn{A x = b}
\deqn{P A = L U}
\deqn{L U x = P b}
\deqn{L y = P b}
\deqn{U x = y}
}
\section{Shapes}{

\itemize{
\item \code{a}: \verb{(n, n)}
\item \code{b}: \verb{(n,)} or \verb{(n, k)}
\item output: same shape as \code{b}
}
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# the solution has `b`'s shape and the operands' common data type
a <- nv_matrix(c(4, 3, 6, 3), nrow = 2, dtype = "f64")
b <- nv_matrix(c(1, 2), nrow = 2, dtype = "f64")
nv_solve(a, b)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_chol]{nv_chol()}}, \code{\link[=nv_triangular_solve]{nv_triangular_solve()}}, \code{\link[=prim_lu]{prim_lu()}}
}
