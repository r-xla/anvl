% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_remainder}
\alias{prim_remainder}
\title{Primitive Remainder}
\usage{
prim_remainder(lhs, rhs)
}
\arguments{
\item{lhs, rhs}{(\code{\link{arrayish}})\cr
Two inputs of the same data type and shape. Can be any numeric data type. R
values assume the other operand's data type when it is in their
\link[=dtypes]{data type category}, and their
\link[=default_dtypes]{default data type} when neither operand has one.}
}
\value{
\code{\link{arrayish}}\cr
Has the inputs' shape and data type.
}
\description{
Element-wise remainder.
Result has sign of the divident, which differs from base R's \code{\%\%}, which is available
via \code{\link[=nv_mod]{nv_mod()}} and has sign of divisor.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_remainder]{hlo_remainder()}}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# two R values: both take an R double's default data type
prim_remainder(1, -3)

# the R value is built at the array's data type instead
prim_remainder(1, nv_scalar(-3, "f64"))

# the sign follows the dividend, where base R's \%\% follows the divisor
1 \%\% -3
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_remainder]{nv_remainder()}}
}
