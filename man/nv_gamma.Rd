% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_gamma}
\alias{nv_gamma}
\alias{gamma.AnvlArray}
\title{Gamma Function}
\usage{
nv_gamma(x)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array. An integer array is computed at the default float data type
(see \code{\link[=default_dtypes]{default_dtypes()}}), the way \code{\link[base:sqrt]{base::sqrt()}} returns a double for an
integer vector. A boolean array is not accepted.}
}
\value{
\code{\link{arrayish}}\cr
Has the same shape as the input, and its data type -- or the default float
data type if the input was an integer array.
}
\description{
Element-wise gamma function. You can also use \code{gamma()}.

XLA has only the log-gamma function, so this is computed as
\code{exp(lgamma(x))} -- via Euler's reflection formula for a negative argument
-- and is therefore less accurate than base R's \code{\link[base:gamma]{base::gamma()}}. It is \code{NaN}
at the poles, i.e. at every whole number that is not positive.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
gamma(nv_array(c(0.5, 1, 5, -1.5)))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_lgamma]{nv_lgamma()}}, which is what the hardware computes.
}
