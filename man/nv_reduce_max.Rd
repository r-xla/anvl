% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_reduce_max}
\alias{nv_reduce_max}
\alias{max.AnvlArray}
\title{Max Reduction}
\usage{
nv_reduce_max(x, axes = NULL, drop = TRUE, nan_rm = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input array.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reduce. Negative values count from the end, i.e. \code{-1}
refers to the last axis. If \code{NULL} (default), reduces over all
axes, returning a scalar.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop reduced axes.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in floating-point inputs. If \code{FALSE}
(default), \code{NaN} propagates. If \code{TRUE}, \code{NaN} values are skipped.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input.
When \code{drop = TRUE}, the reduced axes are removed.
When \code{drop = FALSE}, the reduced axes are set to 1.
}
\description{
Finds the maximum of array elements along the specified axes.
}
\section{Relation to base R}{

\code{max()} reduces over all axes and, like \code{\link[base:max]{base::max()}}, takes several data
arguments: \code{max(x, y)} is the largest element of both arrays. \code{na.rm}
becomes \code{nan_rm}. Beyond base R, named arguments are passed on, so
\code{max(x, axes = 1L)} reduces a single axis -- but only when \code{x} is the only
data argument.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(1:6, nrow = 2)
nv_reduce_max(x)            # all axes -> scalar
nv_reduce_max(x, axes = 1L)
nv_reduce_max(nv_array(c(1, NaN, 3)))
nv_reduce_max(nv_array(c(1, NaN, 3)), nan_rm = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_reduce_max]{prim_reduce_max()}} for the underlying primitive.
}
