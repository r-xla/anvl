% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_reduce_max}
\alias{nv_reduce_max}
\title{Max Reduction}
\usage{
nv_reduce_max(operand, dims = NULL, drop = TRUE, nan_rm = FALSE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Operand.}

\item{dims}{(\code{integer()} | \code{NULL})\cr
Dimensions to reduce. If \code{NULL} (default), reduces over all
dimensions, returning a scalar.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop reduced dimensions.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in floating-point inputs. If \code{FALSE}
(default), \code{NaN} propagates.
If \code{TRUE}, \code{NaN} values are removed.}
}
\value{
\code{\link{arrayish}}\cr
Has the same data type as the input.
When \code{drop = TRUE}, the reduced dimensions are removed.
When \code{drop = FALSE}, the reduced dimensions are set to 1.
}
\description{
Finds the maximum of array elements along the specified dimensions.
}
\section{NaN handling}{

With \code{nan_rm = FALSE} (default), any \code{NaN} along the reduced axis
propagates to the output. With \code{nan_rm = TRUE}, \code{NaN} values are skipped.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(1:6, nrow = 2)
nv_reduce_max(x)            # all dims -> scalar
nv_reduce_max(x, dims = 1L)
nv_reduce_max(nv_array(c(1, NaN, 3)))
nv_reduce_max(nv_array(c(1, NaN, 3)), nan_rm = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_reduce_max]{prim_reduce_max()}} for the underlying primitive.
}
