% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_concatenate}
\alias{nv_concatenate}
\alias{c.AnvlArray}
\title{Concatenate}
\usage{
nv_concatenate(..., axis = NULL)

\method{c}{AnvlArray}(...)
}
\arguments{
\item{...}{(\code{\link{arrayish}})\cr
Arrays to concatenate. Must have the same shape except along \code{axis}.}

\item{axis}{(\code{integer(1)} | \code{NULL})\cr
Axis along which to concatenate.
Negative values count from the end, i.e. \code{-1} refers to the last axis.
If \code{NULL} (default), assumes all inputs are at most 1-D and concatenates along axis 1.}
}
\value{
\code{\link{arrayish}}\cr
Has the common data type and a shape matching the inputs in all
axes except \code{axis}, which is the sum of input sizes.
}
\description{
Concatenates arrays along an axis. Operands are promoted to a common
data type and scalars are broadcast before concatenation.

You can also use \code{c()}, which flattens its arguments first, like base R.
}
\section{Relation to base R}{

\code{c()} flattens every argument to 1-D and concatenates the results, like
\code{\link[base:c]{base::c()}} does. An anvl array is flattened in row-major order, so for an
input with more than one axis the elements come out in a different order
than base R's column-major flatten would produce (see the "Gotchas"
vignette). Use \code{nv_concatenate()} with an explicit \code{axis} to keep the axes.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(c(1, 2, 3))
y <- nv_array(c(4, 5, 6))
nv_concatenate(x, y)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_concatenate]{prim_concatenate()}} for the underlying primitive.
}
