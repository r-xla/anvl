% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_lu}
\alias{prim_lu}
\title{Primitive LU Decomposition}
\usage{
prim_lu(x)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Matrix of data type floating-point with exactly 2 axes.}
}
\value{
\code{list} of three \code{\link{arrayish}} values: \code{LU} \verb{(m, n)} with the same
dtype as the input; \code{pivots} \verb{(k,)} of dtype \code{i32} with
\code{k = min(m, n)} (1-based row swaps such that row \code{i} was exchanged
with row \code{pivots[i]} during elimination step \code{i}); and \code{permutation}
\verb{(m,)} of dtype \code{i32}, a 1-based permutation vector for \eqn{P} such
that \code{(P \%*\% A)[i, ]} equals \code{A[permutation[i], ]}.
}
\description{
Computes the partial-pivoted LU decomposition of a matrix \code{x}:
\deqn{P A = L U,}
where \eqn{P} is a permutation matrix, \eqn{L} is unit lower triangular,
and \eqn{U} is upper triangular. \code{L} (with implicit unit diagonal) and
\code{U} are packed into a single \code{LU} output matching LAPACK's \code{getrf}
layout. \eqn{P} is returned in two equivalent forms: \code{pivots} (LAPACK's
sequential row-swap encoding) and \code{permutation} (an explicit
permutation vector).
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
}
}

\section{StableHLO}{

Lowers to a \code{"lu"} \code{\link[stablehlo:hlo_custom_call]{hlo_custom_call()}} (backed by LAPACK on
CPU and cuSOLVER on CUDA) for \code{LU} and \code{pivots}, followed by a
\code{\link[stablehlo:hlo_while]{hlo_while()}} loop that converts \code{pivots} to \code{permutation}
in-graph.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(4, 3, 6, 3), nrow = 2, dtype = "f64")
prim_lu(x)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_lu]{nv_lu()}}
}
