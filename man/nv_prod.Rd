% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R, R/api-generics.R
\name{nv_prod}
\alias{nv_prod}
\alias{prod.AnvlArray}
\title{Product Reduction}
\usage{
nv_prod(x, axes = NULL, drop = TRUE, nan_rm = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value materializes at its
\link[=default_dtypes]{default data type}.}

\item{axes}{(\code{integer()} | \code{NULL})\cr
Axes to reduce over. Negative values count from the end, i.e. \code{-1} refers
to the last axis. If \code{NULL} (default), reduces over all axes.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced axes: removed from the output shape if \code{TRUE},
set to 1 if \code{FALSE}.}

\item{nan_rm}{(\code{logical(1)})\cr
How to handle \code{NaN} values in float inputs. If \code{FALSE}
(default), \code{NaN} propagates. If \code{TRUE}, \code{NaN} values are skipped.}
}
\value{
(\code{\link{arrayish}})\cr
Has the input's data type, except a boolean input, which is accumulated
at the default integer data type (see
\code{\link[=default_dtypes]{default_dtypes()}}). The shape is the
input's with the reduced axes removed (\code{drop = TRUE}) or set to 1 (\code{drop = FALSE}).
}
\description{
Multiplies array elements along the specified axes.
A boolean array is multiplied as zeroes and ones, like \code{\link[base:prod]{base::prod()}} does.
}
\section{The \code{prod()} generic}{

\code{prod()} reduces over all axes and, like \code{\link[base:prod]{base::prod()}}, takes several data
arguments: \code{prod(x, y)} is the product of both arrays. \code{na.rm} becomes
\code{nan_rm}. Beyond base R, named arguments are passed on, so
\code{prod(x, axes = 1L)} reduces a single axis -- but only when \code{x} is the only
data argument.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(1:6, nrow = 2)
# no axes given: reduce over all of them
nv_prod(x)

# reducing axis 1 removes it, drop = FALSE keeps it at size 1
nv_prod(x, axes = 1L)
nv_prod(x, axes = 1L, drop = FALSE)

# negative axes count from the end
nv_prod(x, axes = -1L)

# NaN propagates unless nan_rm = TRUE
nv_prod(nv_array(c(2, NaN, 3)))
nv_prod(nv_array(c(2, NaN, 3)), nan_rm = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_prod]{prim_prod()}} for the underlying primitive.
}
