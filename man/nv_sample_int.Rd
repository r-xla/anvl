% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-rng.R
\name{nv_sample_int}
\alias{nv_sample_int}
\title{Sample Integers}
\usage{
nv_sample_int(shape, initial_state, n, dtype = "i32")
}
\arguments{
\item{shape}{(\code{integer()})\cr
Shape.}

\item{initial_state}{(\code{\link{arrayish}})\cr
RNG state (\code{ui64[2]}).}

\item{n}{(\code{integer(1)})\cr
Size of the population, i.e. the integers \code{1} to \code{n} are sampled.}

\item{dtype}{(\code{character(1)} | \code{\link[tengen:DataType]{DataType}})\cr
Data type of the sampled integers. Can be any numeric data type; the drawn
indices are converted to it. Boolean is not one, and is rejected: it
cannot hold an index.}
}
\value{
(named \code{list} of two \code{\link{arrayish}})\cr
Elements \code{state}, the updated RNG state, and \code{values}, the sampled integers
of shape \code{shape} and data type \code{dtype}.
}
\description{
Samples integers from \code{1} to \code{n} with equal probability and with
replacement, analogous to R's \code{sample.int()}.

To sample from a population other than \code{1:n}, use \code{\link[=nv_sample]{nv_sample()}}.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# roll six dice; `state` is the updated RNG state
state <- nv_rng_state(42L)
result <- nv_sample_int(6, state, 6L)
result$values
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_sample]{nv_sample()}} to sample from an arbitrary population.

Other rng:
\code{\link{nv_normal}},
\code{\link[=nv_rbinom]{nv_rbinom()}},
\code{\link[=nv_rng_state]{nv_rng_state()}},
\code{\link[=nv_runif]{nv_runif()}},
\code{\link[=nv_sample]{nv_sample()}}
}
\concept{rng}
