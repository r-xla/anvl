% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rdata.R
\name{has_dtype}
\alias{has_dtype}
\alias{has_dtype_bool}
\alias{has_dtype_int}
\alias{has_dtype_uint}
\alias{has_dtype_float}
\title{Ask What Data Type a Value Has}
\usage{
has_dtype_bool(x, r_ok = FALSE)

has_dtype_int(x, r_ok = FALSE, unsigned = TRUE)

has_dtype_uint(x, r_ok = FALSE)

has_dtype_float(x, r_ok = FALSE)
}
\arguments{
\item{x}{(\code{\link{arrayish}} | \code{\link{AbstractArray}})\cr
The value to ask about.}

\item{r_ok}{(\code{logical(1)})\cr
Whether to answer for a value that has no data type yet, with the one it
would commit to (\code{\link[=peek_dtype]{peek_dtype()}}). \code{FALSE} (default) refuses it instead.}

\item{unsigned}{(\code{logical(1)})\cr
Whether an unsigned integer data type counts as an integer one. \code{TRUE} by
default; \code{has_dtype_uint()} asks for unsigned alone.}
}
\value{
(\code{logical(1)})
}
\description{
Whether the data type of \code{x} is a \code{bool}, an integer, an unsigned integer or
a float. These are the predicates a \code{\link[=promotion_rule]{promote_if()}} rule
asks its inputs about: \code{nv_reduce_sum()} counts a boolean input at \code{i32}
with \verb{promote_if(\\(x) has_dtype_bool(x, r_ok = TRUE), promote_dtype("i32"))}.
}
\details{
A value that has no data type \emph{yet} -- a bare R value, or the \code{\link{RData}} box
standing in for a traced R argument -- is refused rather than answered for,
because the answer would be the data type it \emph{would} commit to and not one it
has. \code{r_ok = TRUE} asks for that answer deliberately, and is what a promotion
rule wants: it is how a boolean input is counted whether it arrives as a
\code{bool} array or as an R \code{logical}.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
has_dtype_bool(nv_array(c(TRUE, FALSE)))
has_dtype_float(nv_array(1.5))
has_dtype_int(nv_array(1L))
# An R value has no data type yet, so it is only answered for on request.
try(has_dtype_float(1.5))
has_dtype_float(1.5, r_ok = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=peek_dtype]{peek_dtype()}}, \link{promotion_rule}, \code{\link[tengen:is_dtype_bool]{tengen::is_dtype_bool()}}
}
