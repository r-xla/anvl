% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_reduce_any}
\alias{prim_reduce_any}
\title{Primitive Any Reduction}
\usage{
prim_reduce_any(x, axes, drop = TRUE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Must be boolean. An R value commits to its
\link[=default_dtypes]{default data type}.}

\item{axes}{(\code{integer()})\cr
Axes to reduce over. Negative values count from the end, i.e. \code{-1} refers
to the last axis.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced axes: removed from the output shape if \code{TRUE},
set to 1 if \code{FALSE}.}
}
\value{
\code{\link{arrayish}}\cr
Has boolean data type, and the input's shape with the reduced axes removed
(\code{drop = TRUE}) or set to 1 (\code{drop = FALSE}).
}
\description{
Performs logical OR along the specified axes.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_reduce]{hlo_reduce()}} with \code{\link[stablehlo:hlo_or]{hlo_or()}} as the reducer.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(TRUE, FALSE, TRUE, TRUE), nrow = 2)
# TRUE where any element is TRUE along axis 1
prim_reduce_any(x, axes = 1L)

# drop = FALSE keeps the reduced axis at size 1 instead
prim_reduce_any(x, axes = 1L, drop = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_reduce_any]{nv_reduce_any()}}
}
