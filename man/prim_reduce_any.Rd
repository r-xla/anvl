% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_reduce_any}
\alias{prim_reduce_any}
\title{Primitive Any Reduction}
\usage{
prim_reduce_any(operand, axes, drop = TRUE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Arrayish value of boolean data type.}

\item{axes}{(\code{integer()})\cr
Axes to reduce over.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced axes from the output shape.
If \code{TRUE}, the reduced axes are removed.
If \code{FALSE}, the reduced axes are set to 1.}
}
\value{
\code{\link{arrayish}}\cr
Boolean array. Never ambiguous.
When \code{drop = TRUE}, the shape is that of \code{operand} with \code{axes} removed.
When \code{drop = FALSE}, the shape is that of \code{operand} with \code{axes} set to 1.
}
\description{
Performs logical OR along the specified axes.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_reduce]{hlo_reduce()}} with \code{\link[stablehlo:hlo_or]{hlo_or()}} as the reducer.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(TRUE, FALSE, TRUE, TRUE), nrow = 2)
prim_reduce_any(x, axes = 1L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_reduce_any]{nv_reduce_any()}}
}
