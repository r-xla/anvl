% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/array.R
\name{as_anvl_array}
\alias{as_anvl_array}
\alias{as_anvl_arrays}
\title{Convert to AnvlArray}
\usage{
as_anvl_array(x, device = NULL)

as_anvl_arrays(...)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Input to standardize.}

\item{device}{(\code{NULL} | \code{\link{device}})\cr
Target device. If \code{x} is an \code{AnvlArray} on a different device, an error
is raised.}

\item{...}{(\code{\link{arrayish}})\cr
Inputs to align.}
}
\value{
(\code{\link{AnvlArray}} for \code{as_anvl_array()}, \code{list} of \code{\link{AnvlArray}}s
for \code{as_anvl_arrays()}).
}
\description{
Use this to canonicalize inputs at the start of a function so it works
both with eager executing and in combination with \code{\link[=jit]{jit()}}.
Use \code{\link[=as_anvl_array]{as_anvl_array()}} for a single input and \code{\link[=as_anvl_arrays]{as_anvl_arrays()}} for multiple inputs.
The latter will also ensure all arrays are from the same backend and live on the same device.
}
\details{
During tracing, \link[=GraphBox]{boxes} are returned as is, and R literals and
arrays are boxed as \code{\link{RDataArray}}s -- values that carry their R data but no
data type yet, and take one at their first use.
During eager mode, R literals and arrays are converted to \code{AnvlArray}s on the specified device.
For \code{AnvlArray} inputs, we check that they live on provided device (if specified).
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
as_anvl_array(1L)
as_anvl_arrays(nv_array(1:3), 1L)
\dontshow{\}) # examplesIf}
}
