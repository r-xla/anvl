% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_reduce_sum}
\alias{prim_reduce_sum}
\title{Primitive Sum Reduction}
\usage{
prim_reduce_sum(x, axes, drop = TRUE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value commits to its
\link[=default_dtypes]{default data type}.}

\item{axes}{(\code{integer()})\cr
Axes to reduce over. Negative values count from the end, i.e. \code{-1} refers
to the last axis.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop the reduced axes: removed from the output shape if \code{TRUE},
set to 1 if \code{FALSE}.}
}
\value{
\code{\link{arrayish}}\cr
Has the input's data type. The shape is the input's with the reduced axes
removed (\code{drop = TRUE}) or set to 1 (\code{drop = FALSE}).
}
\description{
Sums array elements along the specified axes.

A boolean input is reduced with a logical OR, so the result is a boolean
rather than a count. \code{\link[=nv_reduce_sum]{nv_reduce_sum()}} counts instead.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_reduce]{hlo_reduce()}} with \code{\link[stablehlo:hlo_add]{hlo_add()}} as the reducer.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(1:6, nrow = 2)
# reducing axis 1 removes it from the shape, and the data type is kept
prim_reduce_sum(x, axes = 1L)

# drop = FALSE keeps the reduced axis at size 1 instead
prim_reduce_sum(x, axes = 1L, drop = FALSE)

# reducing every axis gives a scalar
prim_reduce_sum(x, axes = c(1L, 2L))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=nv_reduce_sum]{nv_reduce_sum()}}
}
