% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_convert}
\alias{nv_convert}
\title{Convert Data Type}
\usage{
nv_convert(x, dtype)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value commits to its
\link[=default_dtypes]{default data type}.}

\item{dtype}{(\code{character(1)} | \code{\link[tengen:DataType]{DataType}})\cr
Target data type. Can be any data type; the values are converted, so the
result may lose precision or wrap around.}
}
\value{
(\code{\link{arrayish}})\cr
Has the given \code{dtype} and the input's shape.
}
\description{
Converts the elements of an array to a different data type.
Returns the input unchanged if it already has the target type.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
# the values are preserved, the data type changes
x <- nv_array(c(1L, 2L, 3L))
nv_convert(x, dtype = "f32")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_convert]{prim_convert()}} for the underlying primitive.
}
