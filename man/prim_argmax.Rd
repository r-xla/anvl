% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_argmax}
\alias{prim_argmax}
\title{Primitive Argmax}
\usage{
prim_argmax(x, dim, drop = TRUE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
Arrayish value of any data type.}

\item{dim}{(\code{integer(1)})\cr
Dimension along which to find the index of the maximum.}

\item{drop}{(\code{logical(1)})\cr
If \code{TRUE} (default) the reduced dimension is removed; if \code{FALSE} it is
kept with size 1.}
}
\value{
\code{\link{arrayish}} of dtype \code{i32}\cr
Same shape as \code{x} with \code{dim} removed (or set to 1 if
\code{drop = FALSE}).
}
\description{
Returns the index of the maximum value along a single dimension. Ties
are broken by returning the smallest index.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to a variadic \code{\link[stablehlo:hlo_reduce]{hlo_reduce()}} over \verb{(values, indices)}
with a (value > value | (value == value & idx < idx)) selector.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
prim_argmax(nv_array(c(3, 1, 4, 1, 5)), dim = 1L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_argmin]{prim_argmin()}}, \code{\link[=nv_argmax]{nv_argmax()}}
}
