% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_argmax}
\alias{prim_argmax}
\title{Primitive Argmax}
\usage{
prim_argmax(x, axis, drop = TRUE)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
One input. Can be any data type. An R value commits to its
\link[=default_dtypes]{default data type}.}

\item{axis}{(\code{integer(1)})\cr
Axis along which to find the index of the maximum.
Negative values count from the end, i.e. \code{-1} refers to the last axis.}

\item{drop}{(\code{logical(1)})\cr
If \code{TRUE} (default) the reduced axis is removed; if \code{FALSE} it is
kept with size 1.}
}
\value{
(\code{\link{arrayish}})\cr
Has the \link[=default_dtypes]{default integer data type} whatever the input's is, and the
input's shape with \code{axis} removed (\code{drop = TRUE}) or set to 1
(\code{drop = FALSE}).
}
\description{
Returns the index of the maximum value along a single axis. Ties
are broken by returning the smallest index.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to a variadic \code{\link[stablehlo:hlo_reduce]{hlo_reduce()}} over \verb{(values, indices)}
with a (value > value | (value == value & idx < idx)) selector.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
prim_argmax(nv_array(c(3, 1, 4, 1, 5)), axis = 1L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_argmin]{prim_argmin()}}, \code{\link[=nv_argmax]{nv_argmax()}}
}
