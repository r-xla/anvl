% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/primitives.R
\name{prim_pad}
\alias{prim_pad}
\title{Primitive Pad}
\usage{
prim_pad(
  x,
  padding_value,
  edge_padding_low,
  edge_padding_high,
  interior_padding
)
}
\arguments{
\item{x}{(\code{\link{arrayish}})\cr
The array to pad. Can be any data type.
\code{x} and \code{padding_value} must have the same data type. An R value among them assumes the data type of the others when it is in its \link[=dtypes]{data type category}, and its \link[=default_dtypes]{default data type} when none of them has one.}

\item{padding_value}{(\code{\link{arrayish}})\cr
Scalar value to use for padding. Shares \code{x}'s data type -- see \code{x}.}

\item{edge_padding_low}{(\code{integer()})\cr
Amount of padding to add at the start of each axis.}

\item{edge_padding_high}{(\code{integer()})\cr
Amount of padding to add at the end of each axis.}

\item{interior_padding}{(\code{integer()})\cr
Amount of padding to add between elements in each axis.}
}
\value{
(\code{\link{arrayish}})\cr
Has the data type the operands agreed on. Each axis grows by
\code{edge_padding_low + edge_padding_high}, plus \code{interior_padding} between
every pair of elements; negative edge padding trims (see \code{\link[stablehlo:hlo_pad]{hlo_pad()}}).
}
\description{
Pads an array with a given padding value.
}
\section{Implemented Rules}{

\itemize{
\item \code{stablehlo}
\item \code{quickr}
\item \code{reverse}
}
}

\section{StableHLO}{

Lowers to \code{\link[stablehlo:hlo_pad]{hlo_pad()}}, specified under \href{https://openxla.org/stablehlo/spec#pad}{pad}.
}

\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_array(1:3)
# one element before, two after
prim_pad(x, 0L, edge_padding_low = 1L, edge_padding_high = 2L, interior_padding = 0L)

# interior padding goes between the elements
prim_pad(x, 0L, 0L, 0L, 1L)

# negative edge padding trims instead
prim_pad(x, 0L, -1L, 0L, 0L)

# one padding amount per axis
prim_pad(nv_matrix(1:4, nrow = 2), 0L, c(1L, 0L), c(0L, 1L), c(0L, 0L))

# the R padding value is built at x's data type
prim_pad(nv_array(c(1.5, 2.5), dtype = "f64"), 0, 1L, 1L, 0L)
\dontshow{\}) # examplesIf}
}
