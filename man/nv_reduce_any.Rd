% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{nv_reduce_any}
\alias{nv_reduce_any}
\title{Any Reduction}
\usage{
nv_reduce_any(operand, dims = NULL, drop = TRUE)
}
\arguments{
\item{operand}{(\code{\link{arrayish}})\cr
Operand.}

\item{dims}{(\code{integer()} | \code{NULL})\cr
Dimensions to reduce. Negative values count from the end, i.e. \code{-1}
refers to the last dimension. If \code{NULL} (default), reduces over all
dimensions, returning a scalar.}

\item{drop}{(\code{logical(1)})\cr
Whether to drop reduced dimensions.}
}
\value{
\code{\link{arrayish}}\cr
Boolean array.
When \code{drop = TRUE}, the reduced dimensions are removed.
When \code{drop = FALSE}, the reduced dimensions are set to 1.
}
\description{
Performs logical OR along the specified dimensions.
Returns \code{TRUE} if any element is \code{TRUE}.
}
\examples{
\dontshow{if (pjrt::plugins_downloaded()) withAutoprint(\{ # examplesIf}
x <- nv_matrix(c(TRUE, FALSE, TRUE, TRUE), nrow = 2)
nv_reduce_any(x)            # all dims -> scalar
nv_reduce_any(x, dims = 1L)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prim_reduce_any]{prim_reduce_any()}} for the underlying primitive.
}
