# nv_lower_tri / nv_upper_tri Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `nv_lower_tri` / `nv_upper_tri` (and their `_like` variants) — boolean triangular mask constructors matching base R's `lower.tri()` / `upper.tri()` — and collapse the three existing copies of the mask logic onto one internal helper.

**Architecture:** Pure `nv_*` API addition; no new primitive. One unexported helper `tri_mask()` composes `prim_iota` with a comparison. `nv_lower_tri`/`nv_upper_tri` validate and call it; the `_like` variants follow the `nv_eye_like` pattern; `nv_tril`/`nv_triu` become thin wrappers; the reverse rules in `R/rules-reverse.R` drop their private `tril_mask`/`triu_mask` in favour of `tri_mask`.

**Tech Stack:** R, roxygen2, testthat (`describe`/`it`), devtools, `air` (formatting), `jarl` (linting).

**Spec:** `specs/2026-07-09-nv-lower-upper-tri-design.md`
**Branch:** `feat/lower-upper-tri` (already created off `main` at `e124cec7`)
**Issue:** Closes [#195](https://github.com/r-xla/anvl/issues/195)

## Global Constraints

- Working directory is `/Users/sebi/r-xla/anvl`. Every path below is relative to it.
- **Never edit `R/jit-registry.R` by hand.** Its header reads `# Generated by anvl::jit_roclet: do not edit by hand`. It is regenerated from `#' @jit static ...` roxygen tags by `devtools::document()`.
- **Never edit `man/*.Rd` by hand.** Edit roxygen comments and run `devtools::document()`.
- **Never edit `NAMESPACE` by hand.** It is generated by `devtools::document()`.
- The pkgdown config lives at `pkgdown/_pkgdown.yml`, **not** `_pkgdown.yml`.
- Do not write comments explaining how the code used to be. anvl's CLAUDE.md forbids historical commentary.
- For length-1 vectors do not use `c()` — write `1L`, not `c(1L)`.
- Inside `nv_*` functions, pass plain R literals (`0`, `1L`) directly to primitives rather than wrapping them in `nv_scalar()`.
- Tests must not call `set.seed()`; use `withr::local_seed()`. (Not needed here — no randomness.)
- Only test what the wrapper adds. Do not re-test primitive correctness.
- `diagonal` carries numpy's `k` semantics, identical to `nv_tril`/`nv_triu`. Only the *defaults* differ: `nv_lower_tri` defaults to `-1L`, `nv_upper_tri` to `1L`, so each reproduces its base R namesake.
- Element `(i, j)` is `TRUE` when `i >= j - diagonal` (lower) or `i <= j - diagonal` (upper).
- Return dtype is always `bool`. There is no `dtype` argument.

### A note on the stale `add-api-function` skill

`.claude/skills/add-api-function/SKILL.md` instructs you to add a `check_eager()` entry to a "cross-device eager (check_eager)" `describe` block in `tests/testthat/test-api.R`. **That helper does not exist** — `grep -rn check_eager tests/` returns nothing, and there is no such `describe` block. Skip that step. Every other instruction in that skill applies.

---

### Task 1: `tri_mask` helper + `nv_lower_tri` / `nv_upper_tri`

**Files:**
- Modify: `R/api.R` (add `tri_mask`, `nv_lower_tri`, `nv_upper_tri` immediately before `nv_tril` at line 2613)
- Modify: `pkgdown/_pkgdown.yml:262-263`
- Test: `tests/testthat/test-api.R` (add before the `describe("nv_tril", ...)` block at line 874)

**Interfaces:**
- Consumes: `prim_iota(dim, dtype, shape, start, ambiguous, device)` from `R/primitives.R:2188`; `assert_int` and `cli_abort`, both already imported in `R/api.R`.
- Produces:
  - `tri_mask(shape, diagonal, lower, device = NULL)` — unexported. `shape` is `integer(2)`, `diagonal` an already-coerced scalar integer, `lower` a scalar logical. Returns an `arrayish` of dtype `bool`. Tasks 3 and 4 call this.
  - `nv_lower_tri(shape, diagonal = -1L, device = NULL)` — exported, `@jit static 1:3`.
  - `nv_upper_tri(shape, diagonal = 1L, device = NULL)` — exported, `@jit static 1:3`.

- [ ] **Step 1: Write the failing tests**

Insert into `tests/testthat/test-api.R` directly above the existing `describe("nv_tril", {` line (currently line 874):

```r
describe("nv_lower_tri", {
  it("matches base R lower.tri() by default", {
    expect_equal(as_array(nv_lower_tri(c(3, 3))), lower.tri(matrix(0, 3, 3)))
  })
  it("diagonal = 0L matches lower.tri(diag = TRUE)", {
    expect_equal(
      as_array(nv_lower_tri(c(3, 3), diagonal = 0L)),
      lower.tri(matrix(0, 3, 3), diag = TRUE)
    )
  })
  it("supports offsets base R's logical diag cannot express", {
    expected <- outer(1:4, 1:4, function(i, j) i >= j - 2L)
    expect_equal(as_array(nv_lower_tri(c(4, 4), diagonal = 2L)), expected)
  })
  it("supports rectangular shapes", {
    expect_equal(as_array(nv_lower_tri(c(2, 5))), lower.tri(matrix(0, 2, 5)))
    expect_equal(as_array(nv_lower_tri(c(5, 2))), lower.tri(matrix(0, 5, 2)))
  })
  it("returns bool", {
    expect_equal(dtype(nv_lower_tri(c(3, 3))), as_dtype("bool"))
  })
  it("rejects a shape that is not 2-D", {
    expect_error(nv_lower_tri(3), "must have length 2")
  })
  it("rejects a non-integer diagonal", {
    expect_error(nv_lower_tri(c(3, 3), diagonal = "a"))
  })
})

describe("nv_upper_tri", {
  it("matches base R upper.tri() by default", {
    expect_equal(as_array(nv_upper_tri(c(3, 3))), upper.tri(matrix(0, 3, 3)))
  })
  it("diagonal = 0L matches upper.tri(diag = TRUE)", {
    expect_equal(
      as_array(nv_upper_tri(c(3, 3), diagonal = 0L)),
      upper.tri(matrix(0, 3, 3), diag = TRUE)
    )
  })
  it("supports offsets base R's logical diag cannot express", {
    expected <- outer(1:4, 1:4, function(i, j) i <= j - 2L)
    expect_equal(as_array(nv_upper_tri(c(4, 4), diagonal = 2L)), expected)
  })
  it("supports rectangular shapes", {
    expect_equal(as_array(nv_upper_tri(c(2, 5))), upper.tri(matrix(0, 2, 5)))
    expect_equal(as_array(nv_upper_tri(c(5, 2))), upper.tri(matrix(0, 5, 2)))
  })
  it("returns bool", {
    expect_equal(dtype(nv_upper_tri(c(3, 3))), as_dtype("bool"))
  })
})

describe("nv_lower_tri with quickr backend", {
  it("works on the quickr backend", {
    skip_if_no_quickr()
    local_backend("quickr")
    expect_equal(as_array(nv_lower_tri(c(3, 3))), lower.tri(matrix(0, 3, 3)))
  })
})
```

`local_backend()` is the anvl-sanctioned way to exercise a different backend (see CLAUDE.md); `nv_lower_tri` takes a shape rather than an array, so there is no operand to carry the backend.

- [ ] **Step 2: Run tests to verify they fail**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "^api$")'
```

Expected: FAIL with `could not find function "nv_lower_tri"`.

- [ ] **Step 3: Write the implementation**

Insert into `R/api.R` immediately **before** the `#' @title Lower Triangular Matrix` roxygen block of `nv_tril` (currently line 2613):

```r
# Boolean triangular mask for a 2-D `shape`. `diagonal` must already be a
# scalar integer; `lower` selects the lower (TRUE) or upper (FALSE) triangle.
tri_mask <- function(shape, diagonal, lower, device = NULL) {
  rows <- prim_iota(dim = 1L, dtype = "i32", shape = shape, start = 1L, device = device)
  cols <- prim_iota(dim = 2L, dtype = "i32", shape = shape, start = 1L, device = device)
  if (lower) rows >= cols - diagonal else rows <= cols - diagonal
}

assert_tri_args <- function(shape, diagonal) {
  if (length(shape) != 2L) {
    cli_abort("{.arg shape} must have length 2, not {length(shape)}.")
  }
  if (any(shape < 0L)) {
    cli_abort("{.arg shape} must not contain negative extents.")
  }
  assert_int(diagonal)
}

#' @title Lower Triangular Mask
#' @description
#' Returns a boolean matrix that is `TRUE` on and below the given diagonal,
#' mirroring base R's `lower.tri()`. Use [nv_tril()] to zero out the other
#' triangle of an existing array instead.
#' @template param_shape
#' @param like ([`AnvlArray`])\cr
#'   Existing array whose attributes are used as defaults
#'   (only for `nv_lower_tri_like()`).
#' @param diagonal (`integer(1)`)\cr
#'   Diagonal offset, with the same meaning as in [nv_tril()]. The default
#'   `-1` excludes the main diagonal, matching `lower.tri()`; use `0` to
#'   include it, matching `lower.tri(diag = TRUE)`.
#' @template param_device
#' @return [`arrayish`]\cr
#'   Has the given `shape` and dtype `bool`.
#' @seealso [nv_upper_tri()], [nv_tril()], [prim_iota()] for the underlying primitive.
#' @examplesIf pjrt::plugins_downloaded()
#' nv_lower_tri(c(3, 3))
#' nv_lower_tri(c(3, 3), diagonal = 0L)
#' @export
#' @jit static 1:3
nv_lower_tri <- function(shape, diagonal = -1L, device = NULL) {
  assert_tri_args(shape, diagonal)
  tri_mask(shape, as.integer(diagonal), lower = TRUE, device = device)
}

#' @title Upper Triangular Mask
#' @description
#' Returns a boolean matrix that is `TRUE` on and above the given diagonal,
#' mirroring base R's `upper.tri()`. Use [nv_triu()] to zero out the other
#' triangle of an existing array instead.
#' @template param_shape
#' @param like ([`AnvlArray`])\cr
#'   Existing array whose attributes are used as defaults
#'   (only for `nv_upper_tri_like()`).
#' @param diagonal (`integer(1)`)\cr
#'   Diagonal offset, with the same meaning as in [nv_triu()]. The default
#'   `1` excludes the main diagonal, matching `upper.tri()`; use `0` to
#'   include it, matching `upper.tri(diag = TRUE)`.
#' @template param_device
#' @return [`arrayish`]\cr
#'   Has the given `shape` and dtype `bool`.
#' @seealso [nv_lower_tri()], [nv_triu()], [prim_iota()] for the underlying primitive.
#' @examplesIf pjrt::plugins_downloaded()
#' nv_upper_tri(c(3, 3))
#' nv_upper_tri(c(3, 3), diagonal = 0L)
#' @export
#' @jit static 1:3
nv_upper_tri <- function(shape, diagonal = 1L, device = NULL) {
  assert_tri_args(shape, diagonal)
  tri_mask(shape, as.integer(diagonal), lower = FALSE, device = device)
}
```

- [ ] **Step 4: Add the two constructors to pkgdown**

In `pkgdown/_pkgdown.yml`, the Linear Algebra section currently ends (lines 262-263):

```yaml
      - nv_tril
      - nv_triu
```

Replace with:

```yaml
      - nv_tril
      - nv_triu
      - nv_lower_tri
      - nv_upper_tri
```

Do **not** add the `_like` variants: no `_like` function appears in `pkgdown/_pkgdown.yml` (`grep -c _like pkgdown/_pkgdown.yml` returns `0`); they are covered by the parent topic via `@rdname`.

- [ ] **Step 5: Regenerate docs, NAMESPACE and the jit registry**

```bash
Rscript -e 'devtools::document()'
```

Expected: `NAMESPACE` gains `export(nv_lower_tri)` and `export(nv_upper_tri)`; `R/jit-registry.R` gains `list(name = "nv_lower_tri", static = 1:3)` and `list(name = "nv_upper_tri", static = 1:3)`; `man/nv_lower_tri.Rd` and `man/nv_upper_tri.Rd` are created. Verify:

```bash
grep -E 'nv_(lower|upper)_tri' NAMESPACE R/jit-registry.R
```

- [ ] **Step 6: Run tests to verify they pass**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "^api$")'
```

Expected: PASS (the quickr test skips if quickr is unavailable).

- [ ] **Step 7: Format and lint**

```bash
make format
jarl check .
```

Expected: no diagnostics.

- [ ] **Step 8: Commit**

```bash
git add R/api.R pkgdown/_pkgdown.yml tests/testthat/test-api.R NAMESPACE R/jit-registry.R man/nv_lower_tri.Rd man/nv_upper_tri.Rd
git commit -m "feat(api): add nv_lower_tri() and nv_upper_tri()"
```

---

### Task 2: `nv_lower_tri_like` / `nv_upper_tri_like`

**Files:**
- Modify: `R/api-like.R` (append after `nv_eye_like`, which ends at line 97)
- Test: `tests/testthat/test-api.R` (uncomment the quickr block from Task 1; add a `_like` block)

**Interfaces:**
- Consumes: `nv_lower_tri` / `nv_upper_tri` from Task 1; `like_defaults(like, ...)` from `R/api-like.R:1`, which fills only the arguments you pass it — so omitting `dtype` leaves the result `bool` regardless of `like`'s dtype.
- Produces:
  - `nv_lower_tri_like(like, diagonal = -1L, shape = NULL, device = NULL)` — exported, `@jit static 2:4`.
  - `nv_upper_tri_like(like, diagonal = 1L, shape = NULL, device = NULL)` — exported, `@jit static 2:4`.
  Task 3 calls both.

- [ ] **Step 1: Write the failing tests**

Add, directly after the `describe("nv_lower_tri with quickr backend", ...)` block from Task 1:

```r
describe("nv_lower_tri_like", {
  it("inherits shape from like", {
    x <- nv_fill(0, c(4, 2))
    expect_equal(as_array(nv_lower_tri_like(x)), lower.tri(matrix(0, 4, 2)))
  })
  it("returns bool even when like is a float array", {
    x <- nv_fill(0, c(3, 3), dtype = "f64")
    expect_equal(dtype(nv_lower_tri_like(x)), as_dtype("bool"))
  })
  it("shape overrides like", {
    x <- nv_fill(0, c(3, 3))
    expect_equal(shape(nv_lower_tri_like(x, shape = c(2, 2))), c(2L, 2L))
  })
  it("forwards diagonal", {
    x <- nv_fill(0, c(3, 3))
    expect_equal(
      as_array(nv_lower_tri_like(x, diagonal = 0L)),
      lower.tri(matrix(0, 3, 3), diag = TRUE)
    )
  })
})

describe("nv_upper_tri_like", {
  it("inherits shape from like", {
    x <- nv_fill(0, c(4, 2))
    expect_equal(as_array(nv_upper_tri_like(x)), upper.tri(matrix(0, 4, 2)))
  })
  it("returns bool even when like is a float array", {
    x <- nv_fill(0, c(3, 3), dtype = "f64")
    expect_equal(dtype(nv_upper_tri_like(x)), as_dtype("bool"))
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "^api$")'
```

Expected: FAIL with `could not find function "nv_lower_tri_like"`.

- [ ] **Step 3: Write the implementation**

Append to `R/api-like.R`:

```r
#' @rdname nv_lower_tri
#' @export
#' @jit static 2:4
nv_lower_tri_like <- function(like, diagonal = -1L, shape = NULL, device = NULL) {
  do.call(
    nv_lower_tri,
    c(list(diagonal = diagonal), like_defaults(like, shape = shape, device = device))
  )
}

#' @rdname nv_upper_tri
#' @export
#' @jit static 2:4
nv_upper_tri_like <- function(like, diagonal = 1L, shape = NULL, device = NULL) {
  do.call(
    nv_upper_tri,
    c(list(diagonal = diagonal), like_defaults(like, shape = shape, device = device))
  )
}
```

`like_defaults` is deliberately **not** given `dtype`, so the mask stays `bool`.

- [ ] **Step 4: Add `_like` examples to the parent roxygen blocks**

Because the `_like` functions share their parent's `.Rd` via `@rdname`, their examples belong in the parent roxygen in `R/api.R`. Extend `nv_lower_tri`'s `@examplesIf` block to:

```r
#' @examplesIf pjrt::plugins_downloaded()
#' nv_lower_tri(c(3, 3))
#' nv_lower_tri(c(3, 3), diagonal = 0L)
#' x <- nv_fill(0, shape = c(3, 3))
#' nv_lower_tri_like(x)
```

and `nv_upper_tri`'s to:

```r
#' @examplesIf pjrt::plugins_downloaded()
#' nv_upper_tri(c(3, 3))
#' nv_upper_tri(c(3, 3), diagonal = 0L)
#' x <- nv_fill(0, shape = c(3, 3))
#' nv_upper_tri_like(x)
```

- [ ] **Step 5: Regenerate docs**

```bash
Rscript -e 'devtools::document()'
```

Expected: `NAMESPACE` gains `export(nv_lower_tri_like)` / `export(nv_upper_tri_like)`; `R/jit-registry.R` gains `list(name = "nv_lower_tri_like", static = 2:4)` and the `_upper_` twin; both `_like` functions appear as usage lines in the existing `man/nv_lower_tri.Rd` / `man/nv_upper_tri.Rd`.

- [ ] **Step 6: Run tests to verify they pass**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "^api$")'
```

Expected: PASS, including the quickr block (or skipped if quickr is unavailable).

- [ ] **Step 7: Format, lint, commit**

```bash
make format
jarl check .
git add R/api.R R/api-like.R tests/testthat/test-api.R NAMESPACE R/jit-registry.R man/nv_lower_tri.Rd man/nv_upper_tri.Rd
git commit -m "feat(api): add nv_lower_tri_like() and nv_upper_tri_like()"
```

---

### Task 3: Reimplement `nv_tril` / `nv_triu` on the mask

This is a refactor: **no new tests**. The existing `describe("nv_tril", ...)`, `describe("nv_triu", ...)` and their two quickr blocks (`tests/testthat/test-api.R:874-928`) are the specification, and they must stay green without modification. If any of them needs editing, the refactor is wrong.

**Files:**
- Modify: `R/api.R:2629-2639` (`nv_tril`) and `R/api.R:2657-2667` (`nv_triu`) — line numbers shift by the block added in Task 1; locate by name.

**Interfaces:**
- Consumes: `nv_lower_tri_like` / `nv_upper_tri_like` from Task 2.
- Produces: no signature change. `nv_tril(operand, diagonal = 0L)` and `nv_triu(operand, diagonal = 0L)` keep `@jit static 2L` and their existing behaviour exactly.

- [ ] **Step 1: Confirm the existing tests pass before you touch anything**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "^api$")'
```

Expected: PASS. This is your baseline.

- [ ] **Step 2: Replace the two function bodies**

The current body of `nv_tril` is:

```r
nv_tril <- function(operand, diagonal = 0L) {
  operand <- as_anvl_array(operand)
  if (ndims(operand) != 2L) {
    cli_abort("operand must be a 2-D array")
  }
  assert_int(diagonal)
  rows <- nv_iota_like(operand, dim = 1L, dtype = "i32")
  cols <- nv_iota_like(operand, dim = 2L, dtype = "i32")
  mask <- rows >= cols - as.integer(diagonal)
  nv_ifelse(mask, operand, nv_fill_like(operand, 0))
}
```

Replace it with:

```r
nv_tril <- function(operand, diagonal = 0L) {
  operand <- as_anvl_array(operand)
  if (ndims(operand) != 2L) {
    cli_abort("operand must be a 2-D array")
  }
  nv_ifelse(nv_lower_tri_like(operand, diagonal), operand, nv_fill_like(operand, 0))
}
```

The current body of `nv_triu` is:

```r
nv_triu <- function(operand, diagonal = 0L) {
  operand <- as_anvl_array(operand)
  if (ndims(operand) != 2L) {
    cli_abort("operand must be a 2-D array")
  }
  assert_int(diagonal)
  rows <- nv_iota_like(operand, dim = 1L, dtype = "i32")
  cols <- nv_iota_like(operand, dim = 2L, dtype = "i32")
  mask <- rows <= cols - as.integer(diagonal)
  nv_ifelse(mask, operand, nv_fill_like(operand, 0))
}
```

Replace it with:

```r
nv_triu <- function(operand, diagonal = 0L) {
  operand <- as_anvl_array(operand)
  if (ndims(operand) != 2L) {
    cli_abort("operand must be a 2-D array")
  }
  nv_ifelse(nv_upper_tri_like(operand, diagonal), operand, nv_fill_like(operand, 0))
}
```

`assert_int(diagonal)` is dropped from both because `nv_lower_tri` / `nv_upper_tri` already validate it via `assert_tri_args`. Leave the `ndims` check where it is: it produces the `"operand must be a 2-D array"` message the existing tests and users rely on, which is more specific than `assert_tri_args`'s shape-length error.

Leave the roxygen blocks of both functions untouched apart from adding the new `@seealso` links:
- `nv_tril`'s `@seealso [nv_triu()]` becomes `@seealso [nv_triu()], [nv_lower_tri()]`
- `nv_triu`'s `@seealso [nv_tril()]` becomes `@seealso [nv_tril()], [nv_upper_tri()]`

- [ ] **Step 3: Run the tests to verify they still pass**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "^api$")'
```

Expected: PASS, with the same test count as the Step 1 baseline. Pay particular attention to `nv_tril`'s "supports positive diagonal offset" (`diagonal = 1L`) and "supports negative diagonal offset" (`diagonal = -1L`) — these confirm that `nv_lower_tri_like(operand, diagonal)` forwards `diagonal` with unchanged meaning even though its *default* differs from `nv_tril`'s.

- [ ] **Step 4: Verify `nv_lu` still works**

`nv_lu` (`R/api.R:1882`, `:1895`) is the only internal caller of `nv_tril`/`nv_triu`.

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "^api$")' 2>&1 | grep -iE 'nv_lu|fail'
```

Expected: no failures.

- [ ] **Step 5: Regenerate docs, format, lint, commit**

```bash
Rscript -e 'devtools::document()'
make format
jarl check .
git add R/api.R man/nv_tril.Rd man/nv_triu.Rd
git commit -m "refactor(api): build nv_tril()/nv_triu() on the triangular masks"
```

---

### Task 4: Collapse the reverse-rule mask helpers

**Files:**
- Modify: `R/rules-reverse.R:1246-1272` (delete `tril_mask` / `triu_mask`, rewrite `triangular_mask`)
- Modify: `R/rules-reverse.R:1303` (the `phi()` closure inside the `prim_chol` reverse rule)
- Modify: `R/rules-reverse.R:1376` and `R/rules-reverse.R:1397` (the two `triangular_mask` call sites)

**Interfaces:**
- Consumes: `tri_mask(shape, diagonal, lower, device = NULL)` from Task 1. It is unexported and lives in the same package namespace, so call it directly — anvl's CLAUDE.md forbids `:::` for the package's own objects.
- Produces: `triangular_mask(n, lower, unit_diagonal)` — the unused `dt` parameter is **removed**. `diag_mask(n)` is unchanged. Both are unexported; nothing outside the package can break.

Why `tri_mask` and not `nv_lower_tri`: every reverse rule stays on `prim_*`, and `nv_lower_tri` carries a `@jit` tag, so calling it here would pull jit-registry dispatch into reverse-mode tracing. `tri_mask` is the shared definition without that coupling.

- [ ] **Step 1: Confirm the gradient tests pass before you touch anything**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "primitives-reverse")'
```

Expected: PASS. This is your baseline — these tests are the specification for this task, and no new tests are added.

- [ ] **Step 2: Delete `tril_mask` and `triu_mask`, rewrite `triangular_mask`**

The current code at `R/rules-reverse.R:1246-1272` is:

```r
# Helper: create a lower triangular mask for an n x n matrix (includes diagonal)
tril_mask <- function(n, dt) {
  rows <- prim_iota(dim = 1L, dtype = "i32", shape = c(n, n), start = 0L)
  cols <- prim_iota(dim = 2L, dtype = "i32", shape = c(n, n), start = 0L)
  prim_ge(rows, cols)
}

# Helper: create an upper triangular mask for an n x n matrix (includes diagonal)
triu_mask <- function(n, dt) {
  rows <- prim_iota(dim = 1L, dtype = "i32", shape = c(n, n), start = 0L)
  cols <- prim_iota(dim = 2L, dtype = "i32", shape = c(n, n), start = 0L)
  rows <= cols
}

diag_mask <- function(n) {
  prim_iota(dim = 1L, dtype = "i32", shape = c(n, n), start = 0L) ==
    prim_iota(dim = 2L, dtype = "i32", shape = c(n, n), start = 0L)
}

triangular_mask <- function(n, dt, lower, unit_diagonal) {
  mask <- if (lower) tril_mask(n, dt) else triu_mask(n, dt)
  if (unit_diagonal) {
    prim_ifelse(diag_mask(n), prim_fill(FALSE, dtype = "bool", shape = c(n, n)), mask)
  } else {
    mask
  }
}
```

Note that `tril_mask` and `triu_mask` both take a `dt` argument that neither uses, and that they have drifted apart (`prim_ge(rows, cols)` vs `rows <= cols`). Replace the whole block with:

```r
diag_mask <- function(n) {
  prim_iota(dim = 1L, dtype = "i32", shape = c(n, n), start = 0L) ==
    prim_iota(dim = 2L, dtype = "i32", shape = c(n, n), start = 0L)
}

triangular_mask <- function(n, lower, unit_diagonal) {
  mask <- tri_mask(c(n, n), 0L, lower = lower)
  if (unit_diagonal) {
    prim_ifelse(diag_mask(n), prim_fill(FALSE, dtype = "bool", shape = c(n, n)), mask)
  } else {
    mask
  }
}
```

`diagonal = 0L` reproduces the old helpers exactly: `tril_mask` was `rows >= cols` and `triu_mask` was `rows <= cols`, both of which include the main diagonal.

`triangular_mask`'s `dt` parameter is **removed**: it is unused, and anvl's CLAUDE.md forbids carrying unneeded baggage. Both of its call sites pass `dtype(a)` into that slot today and must be updated in Step 3b.

`tri_mask` uses `start = 1L` where the deleted helpers used `start = 0L`. This is immaterial: both operands shift by the same constant and only their relative order is compared.

- [ ] **Step 3: Update the one remaining `tril_mask` call site**

Inside the `prim_chol` reverse rule, `R/rules-reverse.R:1303` currently reads:

```r
    prim_ifelse(tril_mask(n, dtype(x)), x, zeros_like(x))
```

Replace with:

```r
    prim_ifelse(tri_mask(c(n, n), 0L, lower = TRUE), x, zeros_like(x))
```

- [ ] **Step 3b: Update the two `triangular_mask` call sites for the dropped `dt`**

`R/rules-reverse.R:1376` and `R/rules-reverse.R:1397` are identical lines inside the `prim_triangular_solve` reverse rule:

```r
      mask <- triangular_mask(n, dtype(a), lower, unit_diagonal)
```

Replace **both** with:

```r
      mask <- triangular_mask(n, lower, unit_diagonal)
```

- [ ] **Step 4: Verify no stale references remain**

```bash
grep -rn 'tril_mask\|triu_mask' R/ tests/
grep -rn 'triangular_mask(n, dtype' R/
```

Expected: no output from either (exit status 1).

- [ ] **Step 5: Run the gradient tests to verify they still pass**

```bash
Rscript -e 'devtools::load_all(quiet = TRUE); testthat::test_local(filter = "primitives-reverse")'
```

Expected: PASS, same count as the Step 1 baseline. The `prim_chol` and `prim_triangular_solve` reverse rules both route through the rewritten `triangular_mask`, so a sign or off-by-one error in `tri_mask` surfaces here as a gradient mismatch against torch.

- [ ] **Step 6: Format, lint, commit**

```bash
make format
jarl check .
git add R/rules-reverse.R
git commit -m "refactor(rules): reuse tri_mask() in the reverse rules"
```

---

### Task 5: Changelog and full verification

**Files:**
- Modify: `NEWS.md` (insert under the existing `## Features` heading, line 3)

**Interfaces:**
- Consumes: everything from Tasks 1-4.
- Produces: nothing consumed downstream.

- [ ] **Step 1: Add the changelog entry**

`NEWS.md` currently begins:

```markdown
# anvl (development version)

## Features

* `trace_fn()` gained an `optimize` argument controlling which graph
```

Insert a new bullet as the **first** item under `## Features`:

```markdown
* New `nv_lower_tri()` and `nv_upper_tri()` (with `nv_lower_tri_like()` /
  `nv_upper_tri_like()`) return a boolean triangular mask for a given shape,
  mirroring base R's `lower.tri()` / `upper.tri()`. As in base R, the main
  diagonal is excluded by default; pass `diagonal = 0L` to include it. Use
  `nv_tril()` / `nv_triu()` to zero out a triangle of an existing array.
```

- [ ] **Step 2: Run the full test suite**

```bash
Rscript -e 'devtools::test()'
```

Expected: PASS, 0 failures. Report the exact failure count; do not claim success without reading the output.

- [ ] **Step 3: Run the full check, including examples**

```bash
Rscript -e 'devtools::check(document = FALSE, args = c("--no-manual", "--no-build-vignettes"))'
```

Expected: 0 errors, 0 warnings. Undocumented-argument notes on `nv_lower_tri` / `nv_upper_tri` mean the `@param like` tag is missing from one of the two roxygen blocks — fix and re-run `devtools::document()`.

- [ ] **Step 4: Format and lint one final time**

```bash
make format
jarl check .
```

Expected: no diagnostics, no diff from `make format`.

- [ ] **Step 5: Commit**

```bash
git add NEWS.md
git commit -m "docs: changelog for nv_lower_tri()/nv_upper_tri()

Closes #195"
```

---

## Self-Review

**Spec coverage.** Every section of `specs/2026-07-09-nv-lower-upper-tri-design.md` maps to a task: the public API and `diagonal` defaults → Task 1; the `_like` variants and their `bool` dtype → Task 2; `nv_tril`/`nv_triu` as wrappers → Task 3; deleting `tril_mask`/`triu_mask` → Task 4; `pkgdown` → Task 1; `NEWS.md` → Task 5; jit registration → Tasks 1-2 (via `@jit` tags, not by hand); errors → Task 1's `assert_tri_args`; the testing matrix → Tasks 1-2, with the refactor regressions in Tasks 3-4.

**Two corrections to the spec, both applied above.** The spec listed `R/jit-registry.R` as a file to hand-edit; it is generated by `devtools::document()` from `@jit` tags, so no task edits it directly. The spec said `_pkgdown.yml`; the real path is `pkgdown/_pkgdown.yml`.

**One requirement dropped.** The repo's `add-api-function` skill demands a `check_eager()` entry per API function. No such helper exists in the repo. No task adds one.

**Type consistency.** `tri_mask(shape, diagonal, lower, device = NULL)` is defined in Task 1 and called with that exact signature in Tasks 1, 3 (transitively), and 4. `nv_lower_tri_like(like, diagonal, shape, device)` is defined in Task 2 and called as `nv_lower_tri_like(operand, diagonal)` in Task 3. `assert_tri_args(shape, diagonal)` is defined and used only in Task 1.
